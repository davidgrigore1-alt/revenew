diff --git a/src/app/(protected)/dashboard/page.tsx b/src/app/(protected)/dashboard/page.tsx
index 91db532..790bfae 100644
--- a/src/app/(protected)/dashboard/page.tsx
+++ b/src/app/(protected)/dashboard/page.tsx
@@ -1,4 +1,5 @@
 import Link from "next/link";
+import { isRedirectError } from "next/dist/client/components/redirect-error";
 import { ControlCenterViews } from "@/components/dashboard/ControlCenterViews";
 import { CommercialDecisionReview } from "@/components/dashboard/CommercialDecisionReview";
 import { RevenueCommandBrief } from "@/components/dashboard/RevenueCommandBrief";
@@ -24,17 +25,19 @@ import { ExecutionControlCenter } from "@/components/dashboard/ExecutionControlC
 import { buildExecutionControlCenter } from "@/lib/execution-control-center";
 import { deriveFirstValueJourney } from "@/lib/first-value-journey";
 import { GettingStarted } from "@/components/guidance/GettingStarted";
+import { IntegrationBrandIcon } from "@/components/ui/IntegrationBrandIcon";
 
 export const dynamic = "force-dynamic";
 
-export default async function DashboardPage({
-  searchParams = {},
-}: {
-  searchParams?: {
-    view?: string;
-    range?: string;
-  };
-}) {
+export default async function DashboardPage(
+  props: {
+    searchParams?: Promise<{
+      view?: string;
+      range?: string;
+    }>;
+  }
+) {
+  const searchParams = (await props.searchParams) ?? {};
   try {
     if (
       searchParams.view === "executive" ||
@@ -233,15 +236,32 @@ export default async function DashboardPage({
     const implementationReady = [
       {
         label: "Gmail",
+        provider: "gmail" as const,
         status: gmailStatus,
+        active:
+          googleState.connection?.gmailStatus === "connected",
+        attention:
+          googleState.connection?.gmailStatus === "action_required" ||
+          googleState.connection?.gmailStatus === "error",
       },
       {
-        label: "Calendar",
+        label: "Google Calendar",
+        provider: "google_calendar" as const,
         status: calendarStatus,
+        active:
+          googleState.connection?.calendarStatus === "connected",
+        attention:
+          googleState.connection?.calendarStatus === "action_required" ||
+          googleState.connection?.calendarStatus === "error",
       },
       {
         label: "Google Drive",
+        provider: "google_drive" as const,
         status: driveStatus,
+        active:
+          googleState.connection?.driveStatus === "connected",
+        attention:
+          googleState.connection?.driveStatus === "action_required",
       },
     ];
 
@@ -270,7 +290,7 @@ export default async function DashboardPage({
     };
 
     return (
-      <div className="mx-auto w-full max-w-[1600px] px-4 pb-24 sm:px-6 lg:px-8 lg:pb-12">
+      <div className="control-center-canvas app-page mx-auto w-full max-w-[var(--workspace-axis)] px-[var(--page-gutter)] pb-24 lg:pb-12">
         {!isSupabaseConfigured ? (
           <div className="pt-4">
             <DemoNotice />
@@ -293,12 +313,29 @@ export default async function DashboardPage({
         />
 
         {interventionBrief ? (
-          <details className="mt-5 border-b border-[rgb(var(--border))] pb-4">
-            <summary className="focus-ring cursor-pointer text-sm font-semibold">
-              Preg─âtire ╚Öi aprobare interven╚øii
+          <details className="control-center-disclosure group mt-5">
+            <summary className="control-center-disclosure-summary focus-ring flex cursor-pointer list-none items-center justify-between gap-4 marker:hidden">
+              <span className="min-w-0">
+                <span className="block text-sm font-semibold text-[rgb(var(--foreground))]">
+                  Preg─âtire ╚Öi aprobare interven╚øii
+                </span>
+                <span className="mt-0.5 block text-xs leading-5 text-[rgb(var(--text-muted))]">
+                  Revizuie╚Öte interven╚øiile preg─âtite ├«nainte de orice aplicare.
+                </span>
+              </span>
+
+              <span className="inline-flex shrink-0 items-center gap-2.5">
+                <span className="hidden text-[11px] font-semibold text-[rgb(var(--text-muted))] sm:inline group-open:hidden">
+                  Deschide
+                </span>
+                <span className="hidden text-[11px] font-semibold text-[rgb(var(--primary))] sm:group-open:inline">
+                  ├Änchide
+                </span>
+                <span aria-hidden="true" className="control-center-disclosure-chevron">Ôîä</span>
+              </span>
             </summary>
 
-            <div className="pt-4">
+            <div className="control-center-disclosure-content">
               <CommercialInterventions
                 brief={interventionBrief}
               />
@@ -307,31 +344,30 @@ export default async function DashboardPage({
         ) : null}
 
         {interventionBrief ? (
-          <details className="group mt-6 border-y border-[rgb(var(--border))] py-4">
-            <summary className="focus-ring flex cursor-pointer list-none flex-wrap items-center justify-between gap-4 marker:hidden">
+          <details className="control-center-disclosure group mt-4">
+            <summary className="control-center-disclosure-summary focus-ring flex cursor-pointer list-none flex-wrap items-center justify-between gap-4 marker:hidden">
               <span>
                 <span className="block text-sm font-semibold">
                   Continu─â analiza cu ReveNew
                 </span>
 
                 <span className="mt-1 block text-xs text-[rgb(var(--text-muted))]">
-                  ├Äntreab─â ce s-a schimbat, de ce conteaz─â
-                  sau preg─âte╚Öte urm─âtorul pas.
+                  Verific─â schimb─ârile, blocajele ╚Öi urm─âtorul pas sigur.
                 </span>
               </span>
 
-              <span className="rounded-control border border-[rgb(var(--primary-border))] px-3 py-2 text-xs font-semibold text-[rgb(var(--primary))] group-open:hidden">
-                ├Äntreab─â ReveNew ÔåÆ
-              </span>
-
-              <span className="hidden text-xs text-[rgb(var(--text-muted))] group-open:block">
-                ├Änchide analiza
+              <span className="inline-flex items-center gap-2 text-xs font-semibold text-[rgb(var(--primary))]">
+                <span className="group-open:hidden">Deschide analiza</span>
+                <span className="hidden group-open:inline">├Änchide analiza</span>
+                <span aria-hidden="true" className="control-center-disclosure-chevron">Ôîä</span>
               </span>
             </summary>
 
-            <HomeAskSurface
-              greeting={morningBrief.salutation}
-            />
+            <div className="control-center-disclosure-content">
+              <HomeAskSurface
+                greeting={morningBrief.salutation}
+              />
+            </div>
           </details>
         ) : (
           <HomeAskSurface
@@ -339,90 +375,189 @@ export default async function DashboardPage({
           />
         )}
 
-        <div className="mt-8 grid gap-8">
-          <details>
-            <summary className="focus-ring cursor-pointer text-sm font-semibold text-[rgb(var(--text-muted))]">
-              Alte semnale ╚Öi decizii comerciale
+        <div className="mt-5 grid gap-5">
+          <details className="control-center-disclosure group">
+            <summary className="control-center-disclosure-summary focus-ring flex cursor-pointer list-none items-center justify-between gap-4 marker:hidden">
+              <span className="min-w-0">
+                <span className="block text-sm font-semibold text-[rgb(var(--text-secondary))]">
+                  Alte semnale ╚Öi decizii comerciale
+                </span>
+                <span className="mt-0.5 block text-xs leading-5 text-[rgb(var(--text-muted))]">
+                  Semnale secundare care pot schimba ordinea de interven╚øie.
+                </span>
+              </span>
+
+              <span className="inline-flex shrink-0 items-center gap-2.5">
+                <span className="hidden text-[11px] font-semibold text-[rgb(var(--text-muted))] sm:inline group-open:hidden">
+                  Deschide
+                </span>
+                <span className="hidden text-[11px] font-semibold text-[rgb(var(--primary))] sm:group-open:inline">
+                  ├Änchide
+                </span>
+                <span aria-hidden="true" className="control-center-disclosure-chevron">Ôîä</span>
+              </span>
             </summary>
 
-            <div className="mt-4">
+            <div className="control-center-disclosure-content">
               <WorkspaceDecisionQueue
                 queue={visibleDecisionQueue}
               />
             </div>
           </details>
 
-          <div className="border-y border-[rgb(var(--border))]">
-            <section
-              aria-labelledby="implementation-status-title"
-              className="max-w-xl py-4"
-            >
-              <h2
-                id="implementation-status-title"
-                className="text-sm font-semibold"
-              >
-                Context conectat
-              </h2>
+          <div className="control-center-sources-band">
+  <section
+    aria-labelledby="implementation-status-title"
+    className="py-5"
+  >
+    <div className="flex flex-wrap items-start justify-between gap-4">
+      <div className="min-w-0">
+        <div className="flex items-center gap-2">
+          <span
+            className="h-1.5 w-1.5 rounded-full bg-emerald-500"
+            aria-hidden="true"
+          />
 
-              <ul className="mt-4 divide-y divide-[rgb(var(--border))] border-y border-[rgb(var(--border))]">
-                {implementationReady.map(
-                  (integration) => (
-                    <li
-                      key={integration.label}
-                      className="flex items-center justify-between gap-3 py-3 text-sm"
-                    >
-                      <span className="font-medium">
-                        {integration.label}
-                      </span>
-
-                      <span
-                        className={
-                          integration.status.includes(
-                            "Activ",
-                          )
-                            ? "inline-flex min-h-6 shrink-0 items-center rounded-full border border-emerald-700/[0.16] bg-emerald-700/[0.06] px-2 text-[11px] font-semibold leading-none text-emerald-800 dark:border-emerald-300/[0.16] dark:bg-emerald-300/[0.07] dark:text-emerald-200"
-                            : integration.status.includes(
-                                  "Necesit─â aten╚øie",
-                                )
-                              ? "status-pill status-pill-warning"
-                              : "status-pill status-pill-neutral"
-                        }
-                      >
-                        {integration.status}
-                      </span>
-                    </li>
-                  ),
-                )}
-              </ul>
+          <p className="text-[10px] font-bold uppercase tracking-[0.14em] text-[rgb(var(--text-muted))]">
+            Surse autorizate
+          </p>
+        </div>
 
-              {relevantDocuments > 0 ? (
-                <p className="mt-2 text-xs text-[rgb(var(--text-muted))]">
-                  {relevantDocuments}{" "}
-                  {relevantDocuments === 1
-                    ? "document relevant"
-                    : "documente relevante"}{" "}
-                  ├«n contextul evaluat
-                </p>
-              ) : null}
-
-              {lastGoogleSync ? (
-                <p className="mt-3 text-xs text-[rgb(var(--text-muted))]">
-                  Ultima sincronizare ┬À {lastGoogleSync}
-                </p>
-              ) : null}
+        <h2
+          id="implementation-status-title"
+          className="mt-2 text-[15px] font-semibold tracking-[-0.015em] text-[rgb(var(--foreground))]"
+        >
+          Context conectat
+        </h2>
+
+        <p className="mt-1 max-w-xl text-xs leading-5 text-[rgb(var(--text-muted))]">
+          ReveNew folose╚Öte numai sursele disponibile ╚Öi autorizate ├«n
+          spa╚øiul curent de lucru.
+        </p>
+      </div>
 
-              <Link
-                href="/apps"
-                className="focus-ring mt-4 inline-flex rounded text-sm font-semibold text-[rgb(var(--primary))] hover:underline"
-              >
-                Gestioneaz─â aplica╚øiile ÔåÆ
-              </Link>
-            </section>
+      {lastGoogleSync ? (
+        <div className="rounded-full border border-[rgb(var(--border-subtle))] bg-[rgb(var(--surface-subtle))] px-3 py-1.5 text-[10px] font-medium text-[rgb(var(--text-muted))]">
+          Sincronizat ┬À {lastGoogleSync}
+        </div>
+      ) : null}
+    </div>
+
+    <ul className="control-center-source-grid mt-4 grid gap-2.5 sm:grid-cols-3">
+      {implementationReady.map((integration) => (
+        <li
+          key={integration.label}
+          className={[
+            "control-center-source-card group relative overflow-hidden rounded-xl border p-3.5 transition-colors",
+            integration.active
+              ? "border-emerald-700/[0.14] bg-emerald-500/[0.025] dark:border-emerald-300/[0.13] dark:bg-emerald-300/[0.035]"
+              : integration.attention
+                ? "border-amber-500/[0.22] bg-amber-500/[0.035]"
+                : "border-[rgb(var(--border))] bg-[rgb(var(--surface-elevated))]",
+          ].join(" ")}
+        >
+          <div className="flex items-start justify-between gap-3">
+            <IntegrationBrandIcon
+              provider={integration.provider}
+              size="medium"
+              className={
+                integration.active
+                  ? "ring-1 ring-black/[0.025]"
+                  : "opacity-70 grayscale-[0.15]"
+              }
+            />
+
+            <span
+              className={[
+                "inline-flex min-h-6 shrink-0 items-center gap-1.5 rounded-full border px-2 text-[10px] font-semibold leading-none",
+                integration.active
+                  ? "border-emerald-700/[0.16] bg-emerald-700/[0.06] text-emerald-800 dark:border-emerald-300/[0.16] dark:bg-emerald-300/[0.07] dark:text-emerald-200"
+                  : integration.attention
+                    ? "border-amber-500/[0.22] bg-amber-500/[0.07] text-amber-800 dark:text-amber-200"
+                    : "border-[rgb(var(--border))] bg-[rgb(var(--surface-subtle))] text-[rgb(var(--text-muted))]",
+              ].join(" ")}
+            >
+              <span
+                className={[
+                  "h-1.5 w-1.5 rounded-full",
+                  integration.active
+                    ? "bg-emerald-500"
+                    : integration.attention
+                      ? "bg-amber-500"
+                      : "bg-[rgb(var(--text-muted))] opacity-45",
+                ].join(" ")}
+                aria-hidden="true"
+              />
+
+              {integration.status}
+            </span>
+          </div>
+
+          <div className="mt-4">
+            <p className="text-[13px] font-semibold tracking-[-0.01em] text-[rgb(var(--foreground))]">
+              {integration.label}
+            </p>
+
+            <p className="mt-1 text-[10px] leading-4 text-[rgb(var(--text-muted))]">
+              {integration.active
+                ? "Disponibil ├«n contextul comercial autorizat."
+                : integration.attention
+                  ? "Conexiunea necesit─â verificare ├«nainte de utilizare."
+                  : "Sursa nu este disponibil─â ├«n contextul curent."}
+            </p>
           </div>
+
+          {integration.active ? (
+            <div
+              className="pointer-events-none absolute inset-x-3 bottom-0 h-px bg-gradient-to-r from-transparent via-emerald-500/35 to-transparent"
+              aria-hidden="true"
+            />
+          ) : null}
+        </li>
+      ))}
+    </ul>
+
+    <div className="mt-4 flex flex-wrap items-center justify-between gap-3 border-t border-[rgb(var(--border-subtle))] pt-4">
+      <div className="flex flex-wrap items-center gap-x-5 gap-y-2 text-[11px] text-[rgb(var(--text-muted))]">
+        {relevantDocuments > 0 ? (
+          <span className="inline-flex items-center gap-2">
+            <span
+              className="h-1.5 w-1.5 rounded-full bg-blue-500"
+              aria-hidden="true"
+            />
+
+            {relevantDocuments}{" "}
+            {relevantDocuments === 1
+              ? "document relevant"
+              : "documente relevante"}{" "}
+            ├«n context
+          </span>
+        ) : null}
+
+        <span>
+          Starea fiec─ârei surse este verificat─â separat.
+        </span>
+      </div>
+
+      <Link
+        href="/apps"
+        className="focus-ring group inline-flex items-center gap-1.5 rounded-md text-xs font-semibold text-[rgb(var(--primary))]"
+      >
+        Gestioneaz─â aplica╚øiile
+        <span
+          className="transition-transform group-hover:translate-x-0.5"
+          aria-hidden="true"
+        >
+          ÔåÆ
+        </span>
+      </Link>
+    </div>
+  </section>
+</div>
         </div>
 
-        <div className="mt-10 grid w-full gap-8 border-t border-[rgb(var(--border))] pt-6 md:grid-cols-2">
-          <section aria-labelledby="home-today-title">
+        <div className="control-center-lower-grid mt-5 grid w-full gap-4 md:grid-cols-2">
+          <section className="control-center-lower-panel" aria-labelledby="home-today-title">
             <div className="flex items-center justify-between gap-4">
               <h2
                 id="home-today-title"
@@ -471,7 +606,7 @@ export default async function DashboardPage({
             )}
           </section>
 
-          <section aria-labelledby="home-recent-title">
+          <section className="control-center-lower-panel" aria-labelledby="home-recent-title">
             <div className="flex items-center justify-between gap-4">
               <h2
                 id="home-recent-title"
@@ -527,6 +662,8 @@ export default async function DashboardPage({
       </div>
     );
   } catch (error) {
+    if (isRedirectError(error)) throw error;
+
     if (
       searchParams.view === "review" ||
       searchParams.view === "executive"
@@ -567,4 +704,4 @@ export default async function DashboardPage({
       </div>
     );
   }
-}
\ No newline at end of file
+}
diff --git a/src/app/globals.css b/src/app/globals.css
index 81edef7..34a3c7b 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -5,7 +5,7 @@
 :root {
   color-scheme: light;
 
-  /* Light V2 experiment: true white with cool, low-chroma operational layers. */
+  /* A4 neutral operational foundation. */
   --background: 255 255 255;
   --background-soft: 244 247 251;
   --surface: 255 255 255;
@@ -133,6 +133,13 @@
   --space-20: 5rem;
   --space-24: 6rem;
 
+  /* Product axes and density */
+  --page-gutter: clamp(1rem, 2vw, 2rem);
+  --content-axis: 92.5rem;
+  --workspace-axis: 110rem;
+  --row-height-compact: 2.5rem;
+  --row-height-comfortable: 3rem;
+
   /* Shape, borders and elevation */
   --radius-control: 0.5rem;
   --radius-button: 0.625rem;
@@ -397,6 +404,7 @@ body {
 
 /* Product grammar: content carries hierarchy; chrome and containment stay quiet. */
 .product-work-surface { border: 1px solid rgb(var(--border) / .78); border-radius: var(--radius-panel); background: rgb(var(--surface)); }
+.product-grouping-surface { border: 1px solid rgb(var(--border-subtle)); border-radius: var(--radius-panel); background: rgb(var(--surface-subtle)); }
 .product-floating-surface { border: 1px solid rgb(var(--border-strong) / .82); border-radius: var(--radius-overlay); background: rgb(var(--surface-elevated)); box-shadow: var(--shadow-modal); }
 .product-interactive-row {
   transition: background-color var(--motion-fast) var(--ease-standard), border-color var(--motion-fast) var(--ease-standard), color var(--motion-fast) var(--ease-standard);
@@ -428,6 +436,476 @@ body {
   animation: revenew-page-reveal var(--motion-page) var(--ease-standard) both;
 }
 
+/* Authenticated shell: graphite navigation stays stable across product surfaces. */
+:root:not(.dark) .authenticated-sidebar {
+  --sidebar: 46 49 58;
+  --background: 46 49 58;
+  --background-soft: 52 56 65;
+  --surface: 52 56 65;
+  --surface-raised: 57 61 71;
+  --surface-subtle: 60 64 74;
+  --surface-muted: 66 70 81;
+  --surface-selected: 64 69 80;
+  --border: 97 102 113;
+  --border-subtle: 78 83 94;
+  --border-strong: 130 136 148;
+  --foreground: 247 248 250;
+  --text-secondary: 224 227 232;
+  --text-muted: 197 202 210;
+  --text-faint: 164 171 181;
+  --sidebar-foreground: 247 248 250;
+  --interaction-tint: 58 70 88;
+  --interaction-border: 88 124 170;
+  --shadow-card: none;
+  color: rgb(var(--foreground));
+  border-right-color: rgb(30 33 40);
+  box-shadow: inset -1px 0 0 rgb(255 255 255 / .045);
+}
+
+/* Control Center: white application canvas with neutral structural depth. */
+:root:not(.dark) .product-desktop:has(.control-center-canvas) {
+  --background: 255 255 255;
+  --background-soft: 247 247 248;
+  --surface: 255 255 255;
+  --surface-raised: 255 255 255;
+  --surface-subtle: 247 247 248;
+  --surface-muted: 234 234 234;
+  --surface-selected: 231 238 245;
+  --surface-elevated: 255 255 255;
+  --surface-floating: 255 255 255;
+  --border: 226 228 231;
+  --border-subtle: 234 234 234;
+  --divider: 234 234 234;
+  --border-strong: 192 192 192;
+  --foreground: 25 29 36;
+  --text-secondary: 76 84 96;
+  --text-muted: 108 116 128;
+  --text-faint: 142 149 160;
+  --shadow-card: 0 1px 2px rgb(20 25 34 / .035), 0 8px 20px rgb(20 25 34 / .028);
+}
+
+.control-center-canvas {
+  position: relative;
+  min-height: calc(100dvh - 3rem);
+  padding-top: .125rem;
+}
+
+.control-center-primary > header,
+.control-center-truth-strip,
+.control-center-analysis,
+.control-center-interventions,
+.control-center-disclosure,
+.control-center-sources-band,
+.control-center-lower-grid {
+  position: relative;
+  z-index: 1;
+}
+
+.control-center-truth-strip {
+  margin-inline: calc(var(--page-gutter) * -.28);
+  padding-inline: calc(var(--page-gutter) * .28);
+  background: rgb(var(--surface-subtle));
+}
+
+.control-center-metric {
+  box-shadow: inset 0 1px 0 rgb(255 255 255 / .42);
+}
+
+.dark .control-center-metric {
+  box-shadow: inset 0 1px 0 rgb(255 255 255 / .025);
+}
+
+.control-center-analysis {
+  margin-inline: calc(var(--page-gutter) * -.45);
+  padding: 1.25rem calc(var(--page-gutter) * .45) 1.5rem;
+  border-top: 1px solid rgb(var(--border-subtle));
+  background: rgb(var(--surface-subtle));
+}
+
+.control-center-chart-card {
+  box-shadow: var(--shadow-card);
+}
+
+.control-center-chart-plot {
+  overflow: hidden;
+  border: 1px solid rgb(var(--border-subtle));
+  border-radius: var(--radius-control);
+  background: rgb(var(--surface));
+}
+
+.control-center-chart-plot .recharts-cartesian-grid-horizontal line {
+  stroke-opacity: .42;
+}
+
+.control-center-workbench {
+  border-color: rgb(var(--border-strong) / .7);
+  background: rgb(var(--surface-raised));
+  box-shadow: var(--shadow-card);
+}
+
+.control-center-case-list {
+  background: rgb(var(--surface-subtle));
+}
+
+.control-center-case-list > div:first-child {
+  background: rgb(var(--surface-muted));
+}
+
+.control-center-case-list button:not(.control-center-case-selected):hover {
+  background: rgb(var(--surface-raised));
+}
+
+.control-center-case-selected {
+  box-shadow: inset 0 0 0 1px rgb(var(--interaction-border) / .18);
+}
+
+.control-center-case-inspector {
+  background: rgb(var(--surface-raised));
+}
+
+.control-center-next-action {
+  border-color: rgb(var(--gold-300) / .75);
+  background: rgb(var(--gold-50) / .34);
+  box-shadow: inset 3px 0 0 rgb(var(--gold-500) / .76);
+}
+
+.dark .control-center-next-action {
+  border-color: rgb(var(--gold-700) / .46);
+  background: rgb(var(--warning-background) / .38);
+}
+
+.control-center-disclosure {
+  overflow: clip;
+  border: 1px solid rgb(var(--border));
+  border-radius: var(--radius-panel);
+  background: rgb(var(--surface) / .72);
+  transition: border-color var(--motion-fast) var(--ease-standard), background-color var(--motion-fast) var(--ease-standard);
+}
+
+.control-center-disclosure:hover,
+.control-center-disclosure[open] {
+  border-color: rgb(var(--border-strong) / .8);
+  background: rgb(var(--surface));
+}
+
+.control-center-disclosure[open] {
+  box-shadow: inset 3px 0 0 rgb(var(--interaction) / .72);
+}
+
+.control-center-disclosure-summary {
+  display: flex;
+  min-height: 3rem;
+  width: 100%;
+  cursor: pointer;
+  list-style: none;
+  align-items: center;
+  justify-content: space-between;
+  gap: .875rem;
+  padding: .625rem .875rem;
+  border-radius: calc(var(--radius-panel) - 1px);
+  transition:
+    background-color var(--motion-fast) var(--ease-standard),
+    color var(--motion-fast) var(--ease-standard);
+}
+
+.control-center-disclosure-summary::-webkit-details-marker {
+  display: none;
+}
+
+.control-center-disclosure-summary:hover {
+  background: rgb(var(--surface-subtle) / .78);
+}
+
+.control-center-disclosure-chevron {
+  display: inline-flex;
+  position: relative;
+  height: 1.625rem;
+  width: 1.625rem;
+  flex: 0 0 1.625rem;
+  align-items: center;
+  justify-content: center;
+  padding: 0;
+  border: 1px solid rgb(var(--border));
+  border-radius: .5rem;
+  background: rgb(var(--surface-raised));
+  color: rgb(var(--text-secondary));
+  font-size: 0;
+  line-height: 0;
+  transition:
+    border-color var(--motion-fast) var(--ease-standard),
+    background-color var(--motion-fast) var(--ease-standard),
+    color var(--motion-fast) var(--ease-standard);
+}
+
+.control-center-disclosure-chevron::before {
+  content: "";
+  display: block;
+  width: .4rem;
+  height: .4rem;
+  border-right: 1.5px solid currentColor;
+  border-bottom: 1.5px solid currentColor;
+  transform: translateY(-1px) rotate(45deg);
+  transform-origin: 50% 50%;
+  transition: transform var(--motion-content) var(--ease-standard);
+}
+
+.control-center-disclosure-summary:hover .control-center-disclosure-chevron {
+  border-color: rgb(var(--border-strong));
+  color: rgb(var(--foreground));
+}
+
+.control-center-disclosure[open]
+  > .control-center-disclosure-summary
+  .control-center-disclosure-chevron {
+  border-color: rgb(var(--primary) / .38);
+}
+
+.control-center-disclosure[open]
+  > .control-center-disclosure-summary
+  .control-center-disclosure-chevron::before {
+  transform: translateY(1px) rotate(225deg);
+}
+
+:root:not(.dark) .control-center-disclosure {
+  border-color: rgb(222 225 229);
+  background: rgb(255 255 255);
+  box-shadow: 0 1px 2px rgb(20 25 34 / .018);
+}
+
+:root:not(.dark) .control-center-disclosure:hover {
+  border-color: rgb(192 196 202);
+  background: rgb(255 255 255);
+}
+
+:root:not(.dark) .control-center-disclosure[open] {
+  border-color: rgb(190 197 207);
+  background: rgb(255 255 255);
+  box-shadow:
+    inset 2px 0 0 rgb(var(--interaction) / .68),
+    0 4px 14px rgb(20 25 34 / .025);
+}
+
+:root:not(.dark) .control-center-disclosure-summary:hover {
+  background: rgb(247 248 249);
+}
+
+:root:not(.dark) .control-center-disclosure-content {
+  background: rgb(255 255 255);
+}
+
+.control-center-disclosure-content {
+  border-top: 1px solid rgb(var(--border-subtle));
+  padding: 1rem;
+  background: rgb(var(--surface-raised));
+}
+
+.control-center-chart-detail-grid {
+  display: grid;
+  gap: 1rem;
+  padding: .25rem;
+}
+
+.control-center-chart-detail-table {
+  width: 100%;
+  border-spacing: 0;
+  border-collapse: separate;
+  overflow: hidden;
+  border: 1px solid rgb(var(--border-subtle));
+  border-radius: .625rem;
+}
+
+.control-center-chart-detail-table caption {
+  padding: .25rem .75rem .75rem;
+  color: rgb(var(--text-secondary));
+  font-size: var(--font-size-metadata);
+  font-weight: 600;
+  text-align: left;
+}
+
+.control-center-chart-detail-table thead {
+  background: rgb(var(--surface-subtle));
+}
+
+.control-center-chart-detail-table th {
+  padding: .625rem .75rem;
+  border-bottom: 1px solid rgb(var(--border));
+  color: rgb(var(--text-muted));
+  font-size: var(--font-size-metadata);
+  font-weight: 600;
+  text-align: left;
+}
+
+.control-center-chart-detail-table td {
+  padding: .625rem .75rem;
+  border-bottom: 1px solid rgb(var(--border-subtle));
+  color: rgb(var(--text-secondary));
+  vertical-align: top;
+}
+
+.control-center-chart-detail-table tbody tr:last-child td {
+  border-bottom: 0;
+}
+
+.control-center-chart-detail-table tbody tr {
+  transition: background-color var(--motion-fast) var(--ease-standard);
+}
+
+.control-center-chart-detail-table tbody tr:hover {
+  background: rgb(var(--surface-subtle) / .72);
+}
+
+.control-center-chart-detail-table td:nth-child(n + 2),
+.control-center-chart-detail-table th:nth-child(n + 2) {
+  font-variant-numeric: tabular-nums;
+}
+
+.control-center-ask-surface {
+  width: min(100%, 68rem);
+  margin-inline: auto;
+  padding: .25rem 0 .125rem;
+}
+
+.control-center-ask-surface .product-work-surface {
+  box-shadow: none;
+}
+
+:root:not(.dark) .control-center-ask-surface .product-work-surface {
+  border-color: rgb(220 224 229);
+  background: rgb(255 255 255);
+}
+
+.control-center-sources-band {
+  overflow: hidden;
+  border: 1px solid rgb(var(--border));
+  border-radius: var(--radius-panel);
+  background: rgb(var(--surface-subtle));
+  padding-inline: 1.125rem;
+}
+
+.control-center-source-grid {
+  max-width: 68rem;
+}
+
+.control-center-source-card {
+  min-height: 7.5rem;
+  background: rgb(var(--surface-raised));
+  box-shadow: inset 0 1px 0 rgb(255 255 255 / .35);
+}
+
+.dark .control-center-source-card {
+  box-shadow: inset 0 1px 0 rgb(255 255 255 / .02);
+}
+
+/* Control Center premium light-mode refinements ÔÇö intentionally light-only */
+:root:not(.dark) .control-center-source-card {
+  min-height: 7.25rem;
+  border-color: rgb(197 221 212);
+  background: rgb(255 255 255);
+  box-shadow:
+    inset 0 1px 0 rgb(255 255 255),
+    0 2px 10px rgb(20 25 34 / .024);
+  transition:
+    border-color var(--motion-fast) var(--ease-standard),
+    background-color var(--motion-fast) var(--ease-standard),
+    transform var(--motion-fast) var(--ease-standard);
+}
+
+:root:not(.dark) .control-center-source-card:hover {
+  border-color: rgb(158 205 187);
+  transform: translateY(-1px);
+}
+
+:root:not(.dark) .control-center-next-action {
+  border-color: rgb(217 183 94 / .62);
+  background: linear-gradient(180deg, rgb(255 252 243), rgb(255 249 232));
+  box-shadow:
+    inset 3px 0 0 rgb(var(--gold-500) / .82),
+    0 4px 16px rgb(108 79 19 / .035);
+}
+
+:root:not(.dark) .control-center-lower-panel {
+  border-color: rgb(221 224 228);
+  background: rgb(255 255 255);
+  box-shadow: 0 2px 10px rgb(20 25 34 / .018);
+}
+
+:root:not(.dark) .control-center-truth-strip .control-center-metric:first-child {
+  min-width: 11rem;
+  border-color: rgb(var(--interaction-border) / .56);
+  background: rgb(255 255 255);
+  box-shadow: inset 0 0 0 1px rgb(var(--interaction) / .025);
+}
+
+:root:not(.dark) .control-center-truth-strip .control-center-metric:nth-child(2) {
+  border-color: rgb(214 166 107 / .48);
+  background: rgb(255 252 247);
+}
+
+:root:not(.dark) .control-center-truth-strip .control-center-metric:nth-child(3),
+:root:not(.dark) .control-center-truth-strip .control-center-metric:nth-child(4) {
+  background: rgb(255 255 255);
+}
+
+:root:not(.dark) .control-center-disclosure-summary {
+  min-height: 3.35rem;
+}
+
+:root:not(.dark) .control-center-disclosure[open] > .control-center-disclosure-summary {
+  background: rgb(249 250 251);
+}
+
+:root:not(.dark) .control-center-disclosure-content {
+  border-top-color: rgb(226 229 233);
+}
+
+@media (max-width: 767px) {
+  :root:not(.dark) .control-center-source-card:hover {
+    transform: none;
+  }
+}
+
+.control-center-lower-panel {
+  min-width: 0;
+  border: 1px solid rgb(var(--border));
+  border-radius: var(--radius-panel);
+  background: rgb(var(--surface) / .78);
+  padding: 1rem;
+}
+
+.control-center-lower-panel ul {
+  border-top-color: rgb(var(--border-subtle));
+}
+
+@media (max-width: 767px) {
+  .control-center-analysis {
+    margin-inline: calc(var(--page-gutter) * -.22);
+    padding-inline: calc(var(--page-gutter) * .22);
+  }
+
+  .control-center-chart-card {
+    padding: 1rem;
+  }
+
+  .control-center-chart-plot {
+    min-width: 0;
+  }
+
+  .control-center-disclosure-summary,
+  .control-center-disclosure-content,
+  .control-center-sources-band,
+  .control-center-lower-panel {
+    padding-inline: .875rem;
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .control-center-disclosure,
+  .control-center-disclosure-summary,
+  .control-center-disclosure-chevron {
+    transition: none;
+  }
+}
+
 .subtle-grid {
   background-image:
     linear-gradient(rgb(var(--border) / 0.32) 1px, transparent 1px),
@@ -562,6 +1040,145 @@ body {
   }
 }
 
+/* Landing V3: restrained motion communicates flow and priority without hiding content. */
+.marketing-canvas .landing-v3-hero-glow {
+  background:
+    radial-gradient(circle at 50% 6%, rgb(216 189 118 / .13), transparent 34%),
+    linear-gradient(90deg, transparent, rgb(255 255 255 / .018), transparent);
+}
+
+.marketing-canvas .landing-v3-scan {
+  background:
+    linear-gradient(115deg, transparent 28%, rgb(216 189 118 / .1) 48%, transparent 68%),
+    repeating-linear-gradient(90deg, transparent 0 72px, rgb(255 255 255 / .025) 72px 73px);
+  background-size: 220% 100%, auto;
+  animation: landing-v3-scan 7s ease-in-out infinite;
+}
+
+.marketing-canvas .landing-v3-process-line::after {
+  position: absolute;
+  inset: -2px auto auto 0;
+  width: 5px;
+  height: 5px;
+  border-radius: 999px;
+  background: #a66e1a;
+  box-shadow: 0 0 0 5px rgb(216 189 118 / .16);
+  content: "";
+  animation: landing-v3-route 5.5s cubic-bezier(.22, 1, .36, 1) infinite;
+}
+
+.marketing-canvas .landing-v3-workflow-grid {
+  background-image:
+    linear-gradient(rgb(128 116 93 / .07) 1px, transparent 1px),
+    linear-gradient(90deg, rgb(128 116 93 / .07) 1px, transparent 1px);
+  background-size: 28px 28px;
+}
+
+.marketing-canvas .landing-v3-workflow-field {
+  background-image: radial-gradient(rgb(123 108 79 / .16) .65px, transparent .65px);
+  background-size: 18px 18px;
+  mask-image: linear-gradient(to bottom, rgb(0 0 0 / .72), transparent 94%);
+}
+
+.marketing-canvas .landing-v3-workflow-connectors path {
+  fill: none;
+  stroke: #9e875d;
+  stroke-width: 1.35;
+  stroke-linecap: round;
+  marker-end: url(#workflowArrow);
+}
+
+@media (min-width: 1024px) {
+  .marketing-canvas .landing-v3-workflow-build-node {
+    position: absolute;
+    width: 17.5%;
+  }
+
+  .marketing-canvas .landing-v3-workflow-node-trigger { left: 1.5%; top: 26px; }
+  .marketing-canvas .landing-v3-workflow-node-condition { left: 24%; top: 26px; }
+  .marketing-canvas .landing-v3-workflow-node-context { left: 46.5%; top: 26px; }
+  .marketing-canvas .landing-v3-workflow-node-review { left: 69%; top: 26px; width: 20%; }
+  .marketing-canvas .landing-v3-workflow-node-prepared { left: 59%; top: 193px; width: 22%; }
+  .marketing-canvas .landing-v3-workflow-node-missing { left: 82.5%; top: 193px; width: 16%; }
+}
+
+@media (max-width: 1023px) {
+  .marketing-canvas .landing-v3-workflow-build-node:not(:last-child)::after {
+    position: absolute;
+    bottom: -13px;
+    left: 1.65rem;
+    width: 1px;
+    height: 13px;
+    background: #b9aa8e;
+    content: "";
+  }
+}
+
+.marketing-reveal[data-visible="true"] .landing-v3-diagnostic-row,
+.marketing-reveal[data-visible="true"] .landing-v3-flow-step,
+.marketing-reveal[data-visible="true"] .landing-v3-queue-row,
+.marketing-reveal[data-visible="true"] .landing-v3-context-node,
+.marketing-reveal[data-visible="true"] .landing-v3-workflow-node {
+  animation: landing-v3-enter 560ms cubic-bezier(.22, 1, .36, 1) both;
+}
+
+.marketing-reveal[data-visible="true"] .landing-v3-workflow-build-node {
+  animation: landing-v3-workflow-build-enter 460ms cubic-bezier(.22, 1, .36, 1) both;
+}
+
+.marketing-reveal[data-visible="true"] .landing-v3-workflow-connectors path {
+  stroke-dasharray: 1000;
+  stroke-dashoffset: 1000;
+  animation: landing-v3-workflow-draw 900ms cubic-bezier(.22, 1, .36, 1) 180ms forwards;
+}
+
+@keyframes landing-v3-enter {
+  from { opacity: 0; transform: translate3d(0, 12px, 0); }
+  to { opacity: 1; transform: none; }
+}
+
+@keyframes landing-v3-workflow-build-enter {
+  from { opacity: 0; transform: translate3d(0, 7px, 0); }
+  to { opacity: 1; transform: none; }
+}
+
+@keyframes landing-v3-workflow-draw {
+  to { stroke-dashoffset: 0; }
+}
+
+@keyframes landing-v3-scan {
+  0%, 18% { background-position: 115% 0, 0 0; }
+  70%, 100% { background-position: -115% 0, 0 0; }
+}
+
+@keyframes landing-v3-route {
+  0%, 12% { left: 0; opacity: 0; }
+  20% { opacity: 1; }
+  82% { opacity: 1; }
+  92%, 100% { left: calc(100% - 5px); opacity: 0; }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .marketing-canvas .landing-v3-scan,
+  .marketing-canvas .landing-v3-process-line::after,
+  .marketing-canvas .landing-v3-diagnostic-row,
+  .marketing-canvas .landing-v3-flow-step,
+  .marketing-canvas .landing-v3-queue-row,
+  .marketing-canvas .landing-v3-context-node,
+  .marketing-canvas .landing-v3-workflow-node {
+    animation: none !important;
+  }
+
+  .marketing-canvas .landing-v3-workflow-build-node {
+    animation: none !important;
+  }
+
+  .marketing-canvas .landing-v3-workflow-connectors path {
+    animation: none !important;
+    stroke-dashoffset: 0 !important;
+  }
+}
+
 @media print {
   body {
     background: #ffffff;
@@ -1206,3 +1823,423 @@ body {
 @keyframes slide-in-right {
   from { opacity: 0; transform: translateX(18px); }
 }
+
+/* Landing V3 premium stage and pricing are scoped to the public marketing canvas. */
+.marketing-canvas .landing-v3-hero-stage {
+  isolation: isolate;
+}
+
+.marketing-canvas .landing-v3-hero-orbit {
+  background:
+    radial-gradient(ellipse at center, rgb(58 91 155 / .2), transparent 58%),
+    radial-gradient(ellipse at 50% 100%, rgb(216 189 118 / .12), transparent 48%);
+  filter: blur(18px);
+}
+
+.marketing-canvas .landing-v3-hero-product {
+  filter: drop-shadow(0 32px 60px rgb(0 0 0 / .36));
+  transform: perspective(1800px) rotateX(.35deg);
+  transform-origin: center bottom;
+}
+
+.marketing-reveal[data-visible="true"] .landing-v3-hero-signal {
+  animation: landing-v3-stage-left 680ms cubic-bezier(.22, 1, .36, 1) 180ms both;
+}
+
+.marketing-reveal[data-visible="true"] .landing-v3-hero-control {
+  animation: landing-v3-stage-right 680ms cubic-bezier(.22, 1, .36, 1) 280ms both;
+}
+
+.landing-v3-pricing {
+  --surface: 21 22 25;
+  --surface-subtle: 16 17 20;
+  --surface-elevated: 25 26 30;
+  --foreground: 248 248 246;
+  --text-secondary: 218 218 212;
+  --text-muted: 155 158 164;
+  --muted-foreground: 160 164 172;
+  --border: 52 54 60;
+  --border-strong: 76 78 85;
+  --primary: 216 189 118;
+  --primary-strong: 235 211 148;
+  --primary-foreground: 23 19 11;
+  --brand-50: 47 41 27;
+}
+
+.landing-v3-pricing .marketing-pricing-card {
+  border-color: rgb(255 255 255 / .12);
+  background: linear-gradient(150deg, rgb(24 25 29), rgb(14 15 18) 72%);
+  box-shadow: 0 24px 58px rgb(0 0 0 / .3), inset 0 1px rgb(255 255 255 / .035);
+}
+
+.landing-v3-pricing .marketing-pricing-card-featured {
+  border-color: rgb(216 189 118 / .62);
+  background: linear-gradient(150deg, rgb(40 35 24), rgb(18 18 20) 68%);
+  box-shadow: 0 30px 72px rgb(0 0 0 / .46), 0 0 0 1px rgb(216 189 118 / .08);
+}
+
+.landing-v3-pricing .marketing-pricing-card:hover {
+  border-color: rgb(216 189 118 / .5);
+  box-shadow: 0 30px 72px rgb(0 0 0 / .42);
+}
+
+.landing-v3-pricing .marketing-pricing-badge {
+  border-color: rgb(216 189 118 / .55);
+  background: rgb(216 189 118 / .12);
+  color: rgb(235 211 148);
+}
+
+@keyframes landing-v3-stage-left {
+  from { opacity: 0; transform: translate3d(16px, 8px, 0); }
+  to { opacity: 1; transform: none; }
+}
+
+@keyframes landing-v3-stage-right {
+  from { opacity: 0; transform: translate3d(-16px, 8px, 0); }
+  to { opacity: 1; transform: none; }
+}
+
+@media (max-width: 1023px) {
+  .marketing-canvas .landing-v3-hero-product {
+    transform: none;
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .marketing-reveal[data-visible="true"] .landing-v3-hero-signal,
+  .marketing-reveal[data-visible="true"] .landing-v3-hero-control {
+    animation: none !important;
+  }
+}
+
+/* Landing V3 hero stability: core commercial rows must never disappear. */
+.marketing-canvas .marketing-product-demo .marketing-demo-result-row {
+  opacity: 1 !important;
+  transform: none !important;
+  animation: none !important;
+}
+
+/* Landing V3 full hero stability: supporting evidence must remain visible. */
+.marketing-canvas .marketing-product-demo .marketing-demo-answer,
+.marketing-canvas .marketing-product-demo .marketing-demo-context-answer,
+.marketing-canvas .marketing-product-demo .marketing-demo-proof,
+.marketing-canvas .marketing-product-demo .marketing-demo-source-row,
+.marketing-canvas .marketing-product-demo .marketing-demo-source-ready,
+.marketing-canvas .marketing-product-demo .marketing-demo-email,
+.marketing-canvas .marketing-product-demo .marketing-demo-log-step,
+.marketing-canvas .marketing-product-demo .marketing-demo-log-ready,
+.marketing-canvas .marketing-product-demo .marketing-demo-metrics {
+  opacity: 1 !important;
+  transform: none !important;
+  animation: none !important;
+}
+
+/* Landing V3 final hero: one settled, readable enterprise product scene. */
+.marketing-canvas .landing-v3-hero-orbit {
+  background: radial-gradient(ellipse at center, rgb(93 108 137 / .14), transparent 64%);
+  filter: blur(24px);
+}
+
+.marketing-canvas .landing-v3-hero-product {
+  filter: none;
+  transform: none;
+}
+
+.marketing-canvas .landing-v3-device-frame {
+  box-shadow:
+    0 42px 90px rgb(0 0 0 / .46),
+    0 8px 24px rgb(0 0 0 / .3),
+    0 0 0 1px rgb(255 255 255 / .12),
+    inset 0 1px rgb(255 255 255 / .1);
+}
+
+.marketing-canvas .landing-v3-browser-chrome {
+  background: linear-gradient(180deg, #fbfcfe, #f1f4f8);
+  box-shadow: inset 0 -1px rgb(148 163 184 / .12);
+}
+
+.marketing-canvas .landing-v3-floating-surface {
+  overflow: hidden;
+  border: 1px solid rgb(203 211 222 / .96);
+  border-radius: .9rem;
+  background: rgb(255 255 255 / .985);
+  box-shadow:
+    0 24px 56px rgb(0 0 0 / .3),
+    0 0 0 1px rgb(255 255 255 / .08),
+    inset 0 1px rgb(255 255 255 / .9);
+}
+
+.marketing-canvas .landing-v3-audit-surface {
+  border: 1px solid rgb(71 85 105 / .82);
+  border-radius: .9rem;
+  background: linear-gradient(150deg, #0f131b, #080b11 78%);
+  box-shadow:
+    0 26px 58px rgb(0 0 0 / .4),
+    0 0 0 1px rgb(96 165 250 / .06),
+    inset 0 1px rgb(255 255 255 / .045);
+}
+
+.marketing-reveal[data-visible="true"] .marketing-demo-conversation,
+.marketing-reveal[data-visible="true"] .marketing-demo-sources,
+.marketing-reveal[data-visible="true"] .marketing-demo-log {
+  animation: landing-v3-hero-surface-enter 560ms cubic-bezier(.22, 1, .36, 1) both;
+}
+
+.marketing-reveal[data-visible="true"] .marketing-demo-conversation { animation-delay: 120ms; }
+.marketing-reveal[data-visible="true"] .marketing-demo-sources { animation-delay: 210ms; }
+.marketing-reveal[data-visible="true"] .marketing-demo-log { animation-delay: 300ms; }
+
+@keyframes landing-v3-hero-surface-enter {
+  from { opacity: 0; transform: translate3d(0, 10px, 0); }
+  to { opacity: 1; transform: none; }
+}
+
+@media (min-width: 1280px) and (max-width: 1399px) {
+  .marketing-canvas .marketing-demo-conversation { left: -4rem !important; top: 8.75rem !important; width: 10.25rem !important; }
+  .marketing-canvas .marketing-demo-sources { right: -4rem !important; top: 5.5rem !important; width: 9.75rem !important; }
+  .marketing-canvas .marketing-demo-log { right: -4.25rem !important; bottom: 1.75rem !important; width: 11.5rem !important; }
+}
+
+@media (min-width: 1400px) and (max-width: 1599px) {
+  .marketing-canvas .marketing-demo-conversation { left: -7rem !important; top: 9rem !important; width: 12.5rem !important; }
+  .marketing-canvas .marketing-demo-sources { right: -7.25rem !important; top: 5.75rem !important; width: 12.25rem !important; }
+  .marketing-canvas .marketing-demo-log { right: -8rem !important; bottom: 2rem !important; width: 13.75rem !important; }
+}
+
+@media (min-width: 1600px) {
+  .marketing-canvas .marketing-demo-conversation { left: -13.5rem !important; width: 13.75rem !important; }
+  .marketing-canvas .marketing-demo-sources { right: -11.75rem !important; width: 12rem !important; }
+  .marketing-canvas .marketing-demo-log { right: -14.5rem !important; width: 15rem !important; }
+}
+
+/* Every piece of commercial evidence remains stable after the entrance. */
+.marketing-canvas .marketing-product-demo .marketing-demo-question,
+.marketing-canvas .marketing-product-demo .marketing-demo-analyzing,
+.marketing-canvas .marketing-product-demo .marketing-demo-processing,
+.marketing-canvas .marketing-product-demo .marketing-demo-answer,
+.marketing-canvas .marketing-product-demo .marketing-demo-context-answer,
+.marketing-canvas .marketing-product-demo .marketing-demo-command-text,
+.marketing-canvas .marketing-product-demo .marketing-demo-result-row,
+.marketing-canvas .marketing-product-demo .marketing-demo-proof,
+.marketing-canvas .marketing-product-demo .marketing-demo-source-row,
+.marketing-canvas .marketing-product-demo .marketing-demo-source-ready,
+.marketing-canvas .marketing-product-demo .marketing-demo-email,
+.marketing-canvas .marketing-product-demo .marketing-demo-log-step,
+.marketing-canvas .marketing-product-demo .marketing-demo-log-ready,
+.marketing-canvas .marketing-product-demo .marketing-demo-stage,
+.marketing-canvas .marketing-product-demo .marketing-demo-metrics,
+.marketing-canvas .marketing-product-demo .marketing-demo-chart-bar {
+  opacity: 1 !important;
+  transform: none !important;
+  animation: none !important;
+}
+
+.marketing-canvas .marketing-product-demo .marketing-demo-command-text { clip-path: none !important; }
+.marketing-canvas .marketing-product-demo .marketing-demo-chart-bar { transform: scaleY(1) !important; }
+
+@media (prefers-reduced-motion: reduce) {
+  .marketing-reveal[data-visible="true"] .marketing-demo-conversation,
+  .marketing-reveal[data-visible="true"] .marketing-demo-sources,
+  .marketing-reveal[data-visible="true"] .marketing-demo-log {
+    animation: none !important;
+  }
+}
+
+/* === Landing V3 cleanup: remove decorative square grids === */
+
+.marketing-grid,
+.subtle-grid,
+.marketing-workflow-canvas,
+.marketing-canvas .landing-v3-workflow-grid {
+  background-image: none !important;
+}
+
+.marketing-canvas .landing-v3-scan {
+  background:
+    linear-gradient(
+      115deg,
+      transparent 28%,
+      rgb(216 189 118 / 0.11) 48%,
+      transparent 68%
+    ) !important;
+  background-size: 220% 100% !important;
+}
+
+/* optional: if any pseudo-grid still leaks through */
+.marketing-canvas [class*="grid-pattern"],
+.marketing-canvas [class*="square-grid"] {
+  background-image: none !important;
+}
+/* Landing V3 floating hero surfaces: always capture-safe and visible. */
+.marketing-canvas .marketing-demo-conversation,
+.marketing-canvas .marketing-demo-sources,
+.marketing-canvas .marketing-demo-log {
+  opacity: 1 !important;
+  transform: none !important;
+  animation: none !important;
+}
+
+/* Landing V3 hero polish ÔÇö less shadow, more breathing room around floating cards */
+.marketing-canvas .marketing-product-stage .marketing-demo-conversation,
+.marketing-canvas .marketing-product-stage .marketing-demo-sources,
+.marketing-canvas .marketing-product-stage .marketing-demo-log {
+  box-shadow: none !important;
+  filter: none !important;
+}
+
+/* keep the floating cards fully visible and slightly detached from the main demo */
+.marketing-canvas .marketing-product-stage {
+  overflow: visible !important;
+}
+
+.marketing-canvas .landing-v3-hero-product,
+.marketing-canvas .marketing-product-stage .marketing-product-demo {
+  overflow: visible !important;
+}
+
+/* active-state transforms: keep subtle motion, but do not visually push cards into the MacBook */
+.marketing-canvas .marketing-product-story[data-active="true"] .marketing-demo-conversation {
+  transform: translate3d(-6px, -2px, 0) !important;
+}
+
+.marketing-canvas .marketing-product-story[data-active="true"] .marketing-demo-sources {
+  transform: translate3d(6px, -2px, 0) !important;
+}
+
+.marketing-canvas .marketing-product-story[data-active="true"] .marketing-demo-log {
+  transform: translate3d(6px, 3px, 0) !important;
+}
+
+/* desktop / laptop large */
+@media (min-width: 1280px) and (max-width: 1599px) {
+  .marketing-demo-conversation {
+    left: -10.75rem !important;
+    width: 9rem !important;
+  }
+
+  .marketing-demo-sources {
+    right: -10.75rem !important;
+    width: 9rem !important;
+  }
+
+  .marketing-demo-log {
+    right: -12.5rem !important;
+    width: 10.5rem !important;
+  }
+}
+
+/* very large desktop */
+@media (min-width: 1600px) {
+  .marketing-product-stage .marketing-product-demo {
+    width: min(74vw, 1320px);
+  }
+
+  .marketing-demo-conversation {
+    left: -16rem !important;
+    width: 13.5rem !important;
+  }
+
+  .marketing-demo-sources {
+    right: -15rem !important;
+    width: 12rem !important;
+  }
+
+  .marketing-demo-log {
+    right: -18rem !important;
+    width: 15rem !important;
+  }
+}
+
+/* Landing V3 hero separation ÔÇö clearly detach floating surfaces from main product frame */
+.marketing-canvas .marketing-product-stage .marketing-demo-conversation,
+.marketing-canvas .marketing-product-stage .marketing-demo-sources,
+.marketing-canvas .marketing-product-stage .marketing-demo-log,
+.marketing-canvas .marketing-product-stage .marketing-demo-conversation > *,
+.marketing-canvas .marketing-product-stage .marketing-demo-sources > *,
+.marketing-canvas .marketing-product-stage .marketing-demo-log > * {
+  box-shadow: none !important;
+  filter: none !important;
+}
+
+/* Create an obvious black breathing gap around the MacBook/product frame */
+.marketing-canvas .marketing-product-stage .marketing-demo-conversation {
+  transform: translate3d(-32px, 0, 0) !important;
+}
+
+.marketing-canvas .marketing-product-stage .marketing-demo-sources {
+  transform: translate3d(32px, 0, 0) !important;
+}
+
+.marketing-canvas .marketing-product-stage .marketing-demo-log {
+  transform: translate3d(36px, 6px, 0) !important;
+}
+
+/* Give the composition a little more breathing room on very large screens */
+@media (min-width: 1600px) {
+  .marketing-canvas .marketing-product-stage .marketing-demo-conversation {
+    transform: translate3d(-40px, 0, 0) !important;
+  }
+
+  .marketing-canvas .marketing-product-stage .marketing-demo-sources {
+    transform: translate3d(40px, 0, 0) !important;
+  }
+
+  .marketing-canvas .marketing-product-stage .marketing-demo-log {
+    transform: translate3d(44px, 6px, 0) !important;
+  }
+}
+/* Landing V3 definitive hero separation */
+.marketing-canvas .landing-v3-hero-product {
+  filter: none !important;
+}
+
+.marketing-canvas .landing-v3-device-frame,
+.marketing-canvas .landing-v3-floating-surface,
+.marketing-canvas .landing-v3-audit-surface {
+  box-shadow: none !important;
+  filter: none !important;
+}
+
+@media (min-width: 1600px) {
+  .marketing-canvas .marketing-demo-conversation {
+    left: -15.25rem !important;
+  }
+
+  .marketing-canvas .marketing-demo-sources {
+    right: -13.5rem !important;
+  }
+
+  .marketing-canvas .marketing-demo-log {
+    right: -16.5rem !important;
+  }
+}
+
+/* Landing V3 definitive positioning: disable legacy transform offsets */
+.marketing-canvas .marketing-product-stage .marketing-demo-conversation,
+.marketing-canvas .marketing-product-stage .marketing-demo-sources,
+.marketing-canvas .marketing-product-stage .marketing-demo-log {
+  transform: none !important;
+}
+
+/* Landing V3 final hero micro-polish */
+.marketing-canvas .landing-v3-conversation-surface,
+.marketing-canvas .landing-v3-context-surface {
+  border: 1px solid rgb(203 213 225 / 0.62) !important;
+  background: rgb(255 255 255 / 0.985) !important;
+  box-shadow: none !important;
+}
+
+.marketing-canvas .landing-v3-audit-surface {
+  border-color: rgb(100 116 139 / 0.34) !important;
+  box-shadow: none !important;
+}
+
+@media (min-width: 1600px) {
+  .marketing-canvas .marketing-demo-conversation {
+    left: -15.625rem !important;
+  }
+
+  .marketing-canvas .marketing-demo-log {
+    bottom: 1rem !important;
+  }
+}
diff --git a/src/components/dashboard/ControlCenterVisuals.tsx b/src/components/dashboard/ControlCenterVisuals.tsx
index c0711f1..7aef486 100644
--- a/src/components/dashboard/ControlCenterVisuals.tsx
+++ b/src/components/dashboard/ControlCenterVisuals.tsx
@@ -87,13 +87,13 @@ const compositionLabel = (
 
 const bucketColors = [
   "rgb(var(--danger-text) / .78)",
-  "rgb(var(--intelligence) / .92)",
+  "rgb(var(--gold-500) / .92)",
   "rgb(var(--text-muted) / .76)",
   "rgb(var(--text-faint) / .72)",
 ];
 
 const card =
-  "flex min-w-0 flex-col rounded-panel border border-[rgb(var(--border))] bg-[rgb(var(--surface-elevated))] p-5";
+  "control-center-chart-card flex min-w-0 flex-col rounded-panel border border-[rgb(var(--border))] bg-[rgb(var(--surface-elevated))] p-5";
 
 const tooltipPanel =
   "min-w-[220px] max-w-[280px] rounded-control border border-[rgb(var(--border-strong))] bg-[rgb(var(--surface-floating))] p-3.5 text-metadata shadow-elevated";
@@ -235,7 +235,7 @@ export function ControlCenterVisuals({
   return (
     <section
       aria-label="Privire comercial─â de ansamblu"
-      className="my-5"
+      className="mt-4"
     >
       <div className="mb-3.5 flex flex-wrap items-center justify-between gap-x-5 gap-y-2">
         <ReportingCurrencyControl
@@ -319,7 +319,7 @@ export function ControlCenterVisuals({
           </figcaption>
 
           <div
-            className={`relative h-[268px] w-full rounded-[14px] bg-[rgb(var(--surface-raised))] ring-1 ring-inset ring-[rgb(var(--border)/0.55)] ${chartInteractionClass}`}
+            className={`control-center-chart-plot relative h-[268px] w-full rounded-[14px] bg-[rgb(var(--surface-raised))] ring-1 ring-inset ring-[rgb(var(--border)/0.55)] ${chartInteractionClass}`}
             role="img"
             aria-label={`Expunere estimat─â ├«n ${currency}, cumulat─â dup─â termenele comerciale actuale. Curba conecteaz─â vizual termene discrete ╚Öi nu reprezint─â istoric de venit.`}
           >
@@ -344,17 +344,17 @@ export function ControlCenterVisuals({
                     >
                       <stop
                         offset="0%"
-                        stopColor="rgb(var(--intelligence))"
+                        stopColor="rgb(var(--gold-500))"
                         stopOpacity={0.16}
                       />
                       <stop
                         offset="55%"
-                        stopColor="rgb(var(--intelligence))"
+                        stopColor="rgb(var(--gold-500))"
                         stopOpacity={0.07}
                       />
                       <stop
                         offset="100%"
-                        stopColor="rgb(var(--intelligence))"
+                        stopColor="rgb(var(--gold-500))"
                         stopOpacity={0.015}
                       />
                     </linearGradient>
@@ -510,20 +510,20 @@ export function ControlCenterVisuals({
                   <Line
                     type="monotoneX"
                     dataKey="cumulative"
-                    stroke="rgb(var(--intelligence))"
-                    strokeWidth={2.5}
+                    stroke="rgb(var(--gold-500))"
+                    strokeWidth={2.25}
                     strokeLinecap="round"
                     strokeLinejoin="round"
                     dot={{
                       r: 3.25,
-                      fill: "rgb(var(--intelligence))",
+                      fill: "rgb(var(--gold-500))",
                       stroke: "rgb(var(--surface))",
                       strokeWidth: 2,
                     }}
                     activeDot={{
                       r: 5,
                       fill: "rgb(var(--surface))",
-                      stroke: "rgb(var(--intelligence))",
+                      stroke: "rgb(var(--gold-500))",
                       strokeWidth: 2.25,
                     }}
                     isAnimationActive={!reduceMotion}
@@ -618,7 +618,7 @@ export function ControlCenterVisuals({
           </figcaption>
 
           <div
-            className={`relative h-[268px] w-full ${chartInteractionClass}`}
+            className={`control-center-chart-plot relative h-[268px] w-full rounded-[14px] bg-[rgb(var(--surface-raised))] ring-1 ring-inset ring-[rgb(var(--border)/0.55)] ${chartInteractionClass}`}
             role="img"
             aria-label={model.buckets
               .map(
@@ -786,21 +786,21 @@ export function ControlCenterVisuals({
         </figure>
       </div>
 
-      <details className="group mt-1 text-metadata text-[rgb(var(--text-muted))]">
-        <summary className="focus-ring inline-flex cursor-pointer list-none items-center gap-1.5 py-1.5 font-semibold text-[rgb(var(--text-secondary))] transition-colors hover:text-[rgb(var(--foreground))]">
+      <details className="control-center-disclosure group mt-2 text-metadata text-[rgb(var(--text-muted))]">
+        <summary className="control-center-disclosure-summary focus-ring flex cursor-pointer list-none items-center justify-between gap-3 font-semibold text-[rgb(var(--text-secondary))] transition-colors hover:text-[rgb(var(--foreground))]">
           <span>Vezi valorile graficului</span>
 
           <span
             aria-hidden="true"
-            className="inline-block text-[11px] transition-transform duration-150 group-open:rotate-180"
+            className="control-center-disclosure-chevron inline-flex h-6 w-6 items-center justify-center text-[11px] transition-transform duration-150"
           >
             Ôû¥
           </span>
         </summary>
 
-        <div className="mt-2 grid gap-5 xl:grid-cols-[1.8fr_1fr]">
-          <div className="overflow-x-auto">
-            <table className="w-full text-left">
+        <div className="control-center-chart-detail-grid grid gap-4 xl:grid-cols-[1.8fr_1fr]">
+          <div className="control-center-chart-detail-table overflow-x-auto rounded-control border border-[rgb(var(--border-subtle))] bg-[rgb(var(--surface))] p-3">
+            <table className="w-full text-left text-metadata">
               <caption className="pb-2 text-left font-medium">
                 Cazuri cu termen ┬À moned─â original─â ╚Öi echivalent analitic
               </caption>
@@ -857,8 +857,8 @@ export function ControlCenterVisuals({
             ) : null}
           </div>
 
-          <div className="overflow-x-auto">
-            <table className="w-full text-left">
+          <div className="control-center-chart-detail-table overflow-x-auto rounded-control border border-[rgb(var(--border-subtle))] bg-[rgb(var(--surface))] p-3">
+            <table className="w-full text-left text-metadata">
               <caption className="pb-2 text-left font-medium">
                 Concentrarea expunerii ┬À{" "}
                 {monetary ? currency : "monede originale"}
@@ -905,4 +905,4 @@ export function ControlCenterVisuals({
       </details>
     </section>
   );
-}
\ No newline at end of file
+}
diff --git a/src/components/dashboard/ExecutionControlCenter.tsx b/src/components/dashboard/ExecutionControlCenter.tsx
index b82a345..5e5a41f 100644
--- a/src/components/dashboard/ExecutionControlCenter.tsx
+++ b/src/components/dashboard/ExecutionControlCenter.tsx
@@ -1,117 +1,838 @@
 "use client";
 
+import type { ReactNode } from "react";
+import { useState } from "react";
+import Link from "next/link";
+import {
+  ArrowRightIcon,
+  ChevronRightIcon,
+} from "@heroicons/react/24/outline";
+
 import type { ReportingFxRate } from "@/lib/reporting-currency";
 import { ControlCenterVisuals } from "@/components/dashboard/ControlCenterVisuals";
 import { CaseReadiness } from "@/components/ui/CaseReadiness";
-import { useState } from "react";
-import Link from "next/link";
-import { ArrowRightIcon, ChevronRightIcon } from "@heroicons/react/24/outline";
 import { Button } from "@/components/ui/Button";
+import { SegmentedFilter } from "@/components/ui/SegmentedFilter";
 import { EvidenceList } from "@/components/evidence/EvidenceList";
-import type { ExecutionCase, ExecutionControlCenterModel } from "@/lib/execution-control-center";
-import { formatProductCurrency, formatProductDateTime, formatProductDate, presentOpportunityState } from "@/lib/ui/presentation";
+
+import type {
+  ExecutionCase,
+  ExecutionControlCenterModel,
+} from "@/lib/execution-control-center";
+
+import {
+  formatProductCurrency,
+  formatProductDateTime,
+  formatProductDate,
+  presentOpportunityState,
+} from "@/lib/ui/presentation";
+
 import { cn } from "@/lib/utils";
 
 const filters = [
-  { id: "all", label: "Toate" }, { id: "attention", label: "Necesit─â aten╚øie" },
-  { id: "overdue", label: "Restante" }, { id: "unassigned", label: "F─âr─â responsabil" }
+  { id: "all", label: "Toate" },
+  { id: "attention", label: "Necesit─â aten╚øie" },
+  { id: "overdue", label: "Restante" },
+  { id: "unassigned", label: "F─âr─â responsabil" },
 ] as const;
-type Filter = typeof filters[number]["id"];
-const matches = (item: ExecutionCase, filter: Filter) => filter === "all" || (filter === "attention" && item.severity !== "informative")
-  || (filter === "overdue" && item.overdue) || (filter === "unassigned" && !item.owner.id);
-const sectionTitle = "text-xs font-semibold text-[rgb(var(--foreground))]";
-const muted = "text-xs leading-5 text-[rgb(var(--text-muted))]";
-
-function CaseDetail({ item,impactHref }: { item: ExecutionCase;impactHref?:string }) {
-  return <article aria-labelledby="selected-execution-title" className="min-w-0 px-5 py-5 xl:px-6">
-    <header className="border-b border-[rgb(var(--border))] pb-4">
-      <p className="text-metadata font-semibold uppercase tracking-[0.1em] text-[rgb(var(--primary))]">Caz selectat</p>
-      <h2 id="selected-execution-title" className="mt-2 break-words text-lg font-semibold leading-6">{item.organization}</h2>
-      <p className="mt-1 break-words text-sm leading-5 text-[rgb(var(--text-secondary))]">{item.opportunityTitle}</p>
-      {impactHref?<Link href={impactHref} className="focus-ring mt-2 inline-block text-xs hover:underline">Impact urm─ârit ┬À Vezi impactul ÔåÆ</Link>:null}
-      <div className="mt-3 flex flex-wrap items-center gap-x-3 gap-y-1 text-xs">
-        <span className="font-semibold tabular-nums">{formatProductCurrency(item.value, item.currency)} <span className="font-normal text-[rgb(var(--text-muted))]">estimat</span></span>
-        <span className="text-[rgb(var(--text-muted))]">{presentOpportunityState(item.status).label}</span>
-      </div>
-    </header>
-
-    <section className="py-4" aria-labelledby="execution-why-now">
-      <h3 id="execution-why-now" className={sectionTitle}>De ce acum</h3>
-      <ul className="mt-2 space-y-2">{item.reasons.slice(0, 4).map((reason) => <li key={reason} className="flex gap-2 text-[13px] leading-5"><span aria-hidden="true" className="mt-2 h-1 w-1 shrink-0 rounded-full bg-[rgb(var(--text-muted))]" />{reason}</li>)}</ul>
-      <details className="mt-3"><summary className="focus-ring cursor-pointer text-metadata text-[rgb(var(--text-muted))]">Cum este stabilit─â prioritatea</summary>
-        <ul className="mt-2 space-y-1">{item.rankingReasons.map((reason) => <li key={reason} className={muted}>{reason}</li>)}{item.reasons.slice(4).map((reason) => <li key={reason} className={muted}>{reason}</li>)}</ul>
-      </details>
-    </section>
 
-    <section className="border-y border-[rgb(var(--border))] py-4" aria-labelledby="execution-next-action">
-      <div className="flex items-baseline justify-between gap-3"><h3 id="execution-next-action" className={sectionTitle}>Urm─âtorul pas</h3><span className="text-metadata text-[rgb(var(--text-muted))]">Control uman</span></div>
-      {item.nextAction ? <div className="mt-2"><p className="text-sm font-medium">{item.nextAction.title}</p><p className={muted}>{item.nextAction.owner ?? "Responsabil de confirmat"} ┬À {formatProductDateTime(item.nextAction.dueAt)}</p></div> : <p className="mt-2 text-sm">Nu exist─â o ac╚øiune urm─âtoare confirmat─â.</p>}
-      <p className="mt-2 text-xs leading-5 text-[rgb(var(--text-muted))]">{item.intervention.explanation}</p>
-      <div className="mt-3 flex flex-wrap items-center gap-3"><Button href={item.intervention.href} size="small">{item.intervention.label}<ArrowRightIcon className="h-3.5 w-3.5" aria-hidden="true" /></Button>
-        <Link href={"/opportunities/" + encodeURIComponent(item.opportunityId)} className="focus-ring rounded text-xs text-[rgb(var(--text-muted))] hover:underline">Deschide oportunitatea</Link>
-      </div>
-      <dl className="mt-4 grid grid-cols-2 gap-x-4 gap-y-3 border-t border-[rgb(var(--border))] pt-3">
-        <div><dt className={muted}>Responsabil comercial</dt><dd className="mt-0.5 break-words text-xs font-medium">{item.owner.name}</dd></div>
-        <div><dt className={muted}>Termen comercial</dt><dd className="mt-0.5 text-xs">{item.deadline ? formatProductDate(item.deadline) : "Neconfirmat"}</dd></div>
-        <div><dt className={muted}>Ultima activitate</dt><dd className="mt-0.5 text-xs">{item.lastActivityAt ? formatProductDate(item.lastActivityAt) : "Neconfirmat─â"}</dd></div>
-        {item.nextMeetingAt ? <div><dt className={muted}>Urm─âtoarea ├«nt├ólnire</dt><dd className="mt-0.5 text-xs">{formatProductDateTime(item.nextMeetingAt)}</dd></div> : null}
+type Filter = (typeof filters)[number]["id"];
+
+const sectionTitle =
+  "text-xs font-semibold tracking-[-0.01em] text-[rgb(var(--foreground))]";
+
+const muted =
+  "text-xs leading-5 text-[rgb(var(--text-muted))]";
+
+function matches(item: ExecutionCase, filter: Filter) {
+  return (
+    filter === "all" ||
+    (filter === "attention" && item.severity !== "informative") ||
+    (filter === "overdue" && item.overdue) ||
+    (filter === "unassigned" && !item.owner.id)
+  );
+}
+
+function Metric({
+  value,
+  label,
+  tone = "neutral",
+}: {
+  value: ReactNode;
+  label: string;
+  tone?: "neutral" | "attention" | "primary";
+}) {
+  return (
+    <div
+      className={cn(
+        "control-center-metric flex min-w-0 items-baseline gap-2 rounded-[0.68rem] border px-3 py-2",
+        "bg-[rgb(var(--surface-raised))]",
+        tone === "attention"
+          ? "border-[rgb(var(--warning-text)/0.28)]"
+          : tone === "primary"
+            ? "border-[rgb(var(--interaction)/0.28)]"
+            : "border-[rgb(var(--border-subtle))]",
+      )}
+    >
+      <strong
+        className={cn(
+          "shrink-0 text-sm font-semibold tabular-nums",
+          tone === "attention"
+            ? "text-[rgb(var(--warning-text))]"
+            : "text-[rgb(var(--foreground))]",
+        )}
+      >
+        {value}
+      </strong>
+
+      <span className="truncate text-metadata text-[rgb(var(--text-muted))]">
+        {label}
+      </span>
+    </div>
+  );
+}
+
+function CaseDetail({
+  item,
+  impactHref,
+}: {
+  item: ExecutionCase;
+  impactHref?: string;
+}) {
+  return (
+    <article
+      aria-labelledby="selected-execution-title"
+      className="control-center-case-detail min-w-0 px-5 py-5 xl:px-6"
+    >
+      <header className="border-b border-[rgb(var(--border))] pb-5">
+        <div className="flex flex-wrap items-center justify-between gap-3">
+          <div className="flex items-center gap-2">
+            <span
+              aria-hidden="true"
+              className="h-1.5 w-1.5 rounded-full bg-[rgb(var(--primary))]"
+            />
+
+            <p className="text-metadata font-semibold uppercase tracking-[0.12em] text-[rgb(var(--primary))]">
+              Caz selectat
+            </p>
+          </div>
+
+          <span className="rounded-full border border-[rgb(var(--border))] bg-[rgb(var(--surface-subtle))] px-2.5 py-1 text-metadata font-medium text-[rgb(var(--text-secondary))]">
+            Control uman
+          </span>
+        </div>
+
+        <h2
+          id="selected-execution-title"
+          className="mt-3 break-words text-lg font-semibold leading-6 tracking-[-0.018em]"
+        >
+          {item.organization}
+        </h2>
+
+        <p className="mt-1 break-words text-sm leading-5 text-[rgb(var(--text-secondary))]">
+          {item.opportunityTitle}
+        </p>
+
+        {impactHref ? (
+          <Link
+            href={impactHref}
+            className="focus-ring mt-2 inline-flex rounded text-xs text-[rgb(var(--text-secondary))] hover:text-[rgb(var(--foreground))] hover:underline"
+          >
+            Impact urm─ârit ┬À Vezi impactul ÔåÆ
+          </Link>
+        ) : null}
+
+        <div className="mt-4 flex flex-wrap items-center gap-2">
+          <span className="rounded-full border border-[rgb(var(--primary)/0.26)] bg-[rgb(var(--primary)/0.07)] px-2.5 py-1 text-xs font-semibold tabular-nums">
+            {formatProductCurrency(item.value, item.currency)}
+          </span>
+
+          <span className="text-xs text-[rgb(var(--text-muted))]">
+            estimat
+          </span>
+
+          <span
+            aria-hidden="true"
+            className="h-1 w-1 rounded-full bg-[rgb(var(--border-strong))]"
+          />
+
+          <span className="text-xs text-[rgb(var(--text-secondary))]">
+            {presentOpportunityState(item.status).label}
+          </span>
+        </div>
+
+        <dl className="control-center-case-facts mt-4 grid grid-cols-2 overflow-hidden rounded-[0.72rem] border border-[rgb(var(--border-subtle))] sm:grid-cols-4">
+          <div className="min-w-0 border-b border-r border-[rgb(var(--border-subtle))] px-3 py-2.5 sm:border-b-0">
+            <dt className="text-micro font-medium uppercase tracking-[0.08em] text-[rgb(var(--text-muted))]">
+              Stare
+            </dt>
+            <dd
+              className={cn(
+                "mt-1 truncate text-xs font-semibold",
+                item.overdue
+                  ? "text-[rgb(var(--warning-text))]"
+                  : "text-[rgb(var(--foreground))]",
+              )}
+            >
+              {item.overdue ? "Termen dep─â╚Öit" : "├Än termen"}
+            </dd>
+          </div>
+
+          <div className="min-w-0 border-b border-[rgb(var(--border-subtle))] px-3 py-2.5 sm:border-b-0 sm:border-r">
+            <dt className="text-micro font-medium uppercase tracking-[0.08em] text-[rgb(var(--text-muted))]">
+              Responsabil
+            </dt>
+            <dd className="mt-1 truncate text-xs font-semibold">
+              {item.owner.name}
+            </dd>
+          </div>
+
+          <div className="min-w-0 border-r border-[rgb(var(--border-subtle))] px-3 py-2.5">
+            <dt className="text-micro font-medium uppercase tracking-[0.08em] text-[rgb(var(--text-muted))]">
+              Scaden╚ø─â
+            </dt>
+            <dd className="mt-1 truncate text-xs font-semibold tabular-nums">
+              {item.deadline ? formatProductDate(item.deadline) : "Neconfirmat─â"}
+            </dd>
+          </div>
+
+          <div className="min-w-0 px-3 py-2.5">
+            <dt className="text-micro font-medium uppercase tracking-[0.08em] text-[rgb(var(--text-muted))]">
+              Dovezi
+            </dt>
+            <dd className="mt-1 text-xs font-semibold tabular-nums">
+              {item.evidence.length}
+            </dd>
+          </div>
+        </dl>
+      </header>
+
+      <section
+        className="py-5"
+        aria-labelledby="execution-why-now"
+      >
+        <div className="flex items-baseline justify-between gap-4">
+          <h3
+            id="execution-why-now"
+            className={sectionTitle}
+          >
+            De ce acum
+          </h3>
+
+          {item.overdue ? (
+            <span className="text-metadata font-medium text-[rgb(var(--warning-text))]">
+              Termen dep─â╚Öit
+            </span>
+          ) : null}
+        </div>
+
+        <ul className="mt-3 space-y-2.5">
+          {item.reasons.slice(0, 3).map((reason) => (
+            <li
+              key={reason}
+              className="flex gap-2.5 text-[13px] leading-5"
+            >
+              <span
+                aria-hidden="true"
+                className="mt-[0.47rem] h-1 w-1 shrink-0 rounded-full bg-[rgb(var(--text-muted))]"
+              />
+
+              <span>{reason}</span>
+            </li>
+          ))}
+        </ul>
+
+        <details className="group mt-4">
+          <summary className="focus-ring flex w-fit cursor-pointer list-none items-center gap-1.5 rounded text-metadata text-[rgb(var(--text-muted))] transition-colors hover:text-[rgb(var(--foreground))]">
+            <ChevronRightIcon
+              aria-hidden="true"
+              className="h-3 w-3 transition-transform duration-fast group-open:rotate-90"
+            />
+            Cum este stabilit─â prioritatea
+          </summary>
+
+          <div className="mt-3 border-l border-[rgb(var(--border))] pl-4">
+            <ul className="space-y-1.5">
+              {item.rankingReasons.map((reason) => (
+                <li
+                  key={reason}
+                  className={muted}
+                >
+                  {reason}
+                </li>
+              ))}
+
+              {item.reasons.slice(4).map((reason) => (
+                <li
+                  key={reason}
+                  className={muted}
+                >
+                  {reason}
+                </li>
+              ))}
+            </ul>
+          </div>
+        </details>
+      </section>
+
+      <section
+        className="control-center-next-action rounded-[0.85rem] border border-[rgb(var(--border))] bg-[rgb(var(--surface-subtle))] p-4"
+        aria-labelledby="execution-next-action"
+      >
+        <div className="flex flex-wrap items-center justify-between gap-3">
+          <div>
+            <p className="text-metadata font-semibold uppercase tracking-[0.11em] text-[rgb(var(--primary))]">
+              Urm─âtorul pas
+            </p>
+
+            <h3
+              id="execution-next-action"
+              className="mt-1 text-sm font-semibold tracking-[-0.01em]"
+            >
+              {item.nextAction
+                ? item.nextAction.title
+                : "Ac╚øiune de confirmat"}
+            </h3>
+          </div>
+
+          <span className="rounded-full border border-[rgb(var(--border))] bg-[rgb(var(--surface))] px-2.5 py-1 text-metadata text-[rgb(var(--text-muted))]">
+            Decizie uman─â
+          </span>
+        </div>
+
+        {item.nextAction ? (
+          <p className="mt-2 text-xs leading-5 text-[rgb(var(--text-secondary))]">
+            {item.nextAction.owner ?? "Responsabil de confirmat"}
+            {" ┬À "}
+            {formatProductDateTime(item.nextAction.dueAt)}
+          </p>
+        ) : (
+          <p className="mt-2 text-xs leading-5 text-[rgb(var(--text-secondary))]">
+            Nu exist─â ├«nc─â o ac╚øiune urm─âtoare confirmat─â.
+          </p>
+        )}
+
+        <p className="mt-3 text-xs leading-5 text-[rgb(var(--text-muted))]">
+          {item.intervention.explanation}
+        </p>
+
+        <dl className="control-center-next-action-facts mt-4 grid gap-px overflow-hidden rounded-control border border-[rgb(var(--gold-300)/0.52)] bg-[rgb(var(--gold-300)/0.34)] sm:grid-cols-3">
+          <div className="min-w-0 bg-[rgb(var(--surface-raised)/0.78)] px-3 py-2.5">
+            <dt className="text-metadata text-[rgb(var(--text-muted))]">Responsabil</dt>
+            <dd className="mt-1 truncate text-xs font-semibold">{item.nextAction?.owner ?? item.owner.name}</dd>
+          </div>
+          <div className="min-w-0 bg-[rgb(var(--surface-raised)/0.78)] px-3 py-2.5">
+            <dt className="text-metadata text-[rgb(var(--text-muted))]">Scaden╚ø─â propus─â</dt>
+            <dd className="mt-1 truncate text-xs font-semibold">{item.nextAction ? formatProductDate(item.nextAction.dueAt) : "De confirmat"}</dd>
+          </div>
+          <div className="min-w-0 bg-[rgb(var(--surface-raised)/0.78)] px-3 py-2.5">
+            <dt className="text-metadata text-[rgb(var(--text-muted))]">Impact estimat</dt>
+            <dd className="mt-1 truncate text-xs font-semibold tabular-nums">{formatProductCurrency(item.value, item.currency)}</dd>
+          </div>
+        </dl>
+
+        <div className="mt-4 flex flex-wrap items-center gap-3">
+          <Button
+            href={item.intervention.href}
+            size="small"
+          >
+            {item.intervention.label}
+
+            <ArrowRightIcon
+              className="h-3.5 w-3.5"
+              aria-hidden="true"
+            />
+          </Button>
+
+          <Link
+            href={`/opportunities/${encodeURIComponent(item.opportunityId)}`}
+            className="focus-ring rounded text-xs text-[rgb(var(--text-muted))] transition-colors hover:text-[rgb(var(--foreground))] hover:underline"
+          >
+            Deschide oportunitatea
+          </Link>
+        </div>
+      </section>
+
+      <dl className="control-center-secondary-facts grid grid-cols-1 gap-3 border-b border-[rgb(var(--border))] py-5 sm:grid-cols-2">
+        <div className="min-w-0 rounded-[0.65rem] border border-[rgb(var(--border-subtle))] bg-[rgb(var(--surface-subtle)/0.58)] px-3 py-2.5">
+          <dt className={muted}>
+            Ultima activitate
+          </dt>
+
+          <dd className="mt-1 text-xs font-medium">
+            {item.lastActivityAt
+              ? formatProductDate(item.lastActivityAt)
+              : "Neconfirmat─â"}
+          </dd>
+        </div>
+
+        <div className="min-w-0 rounded-[0.65rem] border border-[rgb(var(--border-subtle))] bg-[rgb(var(--surface-subtle)/0.58)] px-3 py-2.5">
+          <dt className={muted}>
+            Urm─âtoarea ├«nt├ólnire
+          </dt>
+
+          <dd className="mt-1 text-xs font-medium">
+            {item.nextMeetingAt
+              ? formatProductDateTime(item.nextMeetingAt)
+              : "Neconfirmat─â"}
+          </dd>
+        </div>
       </dl>
-    </section>
-    <section className="pt-4" aria-labelledby="execution-evidence"><h3 id="execution-evidence" className={sectionTitle}>Dovezi <span className="ml-1 font-normal text-[rgb(var(--text-muted))]">┬À {item.evidence.length}</span></h3><EvidenceList items={item.evidence} /></section>
-    <section className="mt-3 border-t border-[rgb(var(--border))] pt-4" aria-labelledby="execution-recent"><h3 id="execution-recent" className={sectionTitle}>Activitate recent─â</h3><EvidenceList items={item.recentActivity} limit={3} label="Activitate recent─â" /></section>
-  </article>;
+
+      <section
+        className="pt-5"
+        aria-labelledby="execution-evidence"
+      >
+        <div className="flex items-baseline gap-2">
+          <h3
+            id="execution-evidence"
+            className={sectionTitle}
+          >
+            Dovezi
+          </h3>
+
+          <span className="text-metadata tabular-nums text-[rgb(var(--text-muted))]">
+            {item.evidence.length}
+          </span>
+        </div>
+
+        <EvidenceList items={item.evidence} />
+      </section>
+
+      <section
+        className="mt-4 border-t border-[rgb(var(--border))] pt-5"
+        aria-labelledby="execution-recent"
+      >
+        <h3
+          id="execution-recent"
+          className={sectionTitle}
+        >
+          Activitate recent─â
+        </h3>
+
+        <EvidenceList
+          items={item.recentActivity}
+          limit={3}
+          label="Activitate recent─â"
+        />
+      </section>
+    </article>
+  );
 }
 
-export function ExecutionControlCenter({ model,impactLinks={},fx,asOf }: { model: ExecutionControlCenterModel;impactLinks?:Record<string,string>;fx:ReportingFxRate|null;asOf:string }) {
+export function ExecutionControlCenter({
+  model,
+  impactLinks = {},
+  fx,
+  asOf,
+}: {
+  model: ExecutionControlCenterModel;
+  impactLinks?: Record<string, string>;
+  fx: ReportingFxRate | null;
+  asOf: string;
+}) {
   const [filter, setFilter] = useState<Filter>("all");
   const [selectedId, setSelectedId] = useState<string | null>(null);
-  const visible = model.cases.filter((item) => matches(item, filter));
-  // Selection is always resolved against the latest props/filter, never stale content.
-  const selected = visible.find((item) => item.id === selectedId) ?? visible[0] ?? null;
-  return <section aria-labelledby="execution-center-title" className="pt-6">
-    <header className="flex flex-wrap items-end justify-between gap-4">
-      <div><h1 id="execution-center-title" className="text-page-heading font-semibold tracking-tight">Control Center</h1><p className="mt-1 text-sm text-[rgb(var(--text-muted))]">Situa╚øiile comerciale care necesit─â interven╚øie</p></div>
-      <Link href="/opportunities" className="focus-ring rounded text-xs text-[rgb(var(--text-muted))] hover:underline">Toate oportunit─â╚øile ÔåÆ</Link>
-    </header>
-    <div className="mt-5 flex flex-wrap gap-x-6 gap-y-2 border-y border-[rgb(var(--border))] py-3 text-xs">
-      <span><strong className="tabular-nums">{model.cases.length}</strong> <span className="text-[rgb(var(--text-muted))]">{model.cases.length === 1 ? "situa╚øie" : "situa╚øii"} de revizuit</span></span>
-      <span><strong className="tabular-nums">{model.overdueCount}</strong> <span className="text-[rgb(var(--text-muted))]">cu termen dep─â╚Öit</span></span>
-      {Object.keys(model.exposure).length ? <span className="flex flex-wrap gap-x-2"><span className="text-[rgb(var(--text-muted))]">Expunere estimat─â</span>{Object.entries(model.exposure).map(([currency, amount]) => <strong className="tabular-nums" key={currency}>{formatProductCurrency(amount, currency)}</strong>)}</span> : null}
-    </div>
-    <ControlCenterVisuals cases={model.cases} fx={fx} asOf={asOf}/>
-    {model.sourceState === "fallback" ? <p role="status" className="mt-2 text-xs leading-5 text-[rgb(var(--text-muted))]">Afi╚Ö─âm evaluarea disponibil─â din ├«nregistr─âri. Preg─âtirea interven╚øiilor nu este disponibil─â momentan.</p> : null}
-    <div role="group" aria-label="Filtreaz─â situa╚øiile" className="flex flex-wrap gap-1 border-b border-[rgb(var(--border))] py-3">
-      {filters.map((option) => <button key={option.id} type="button" aria-pressed={filter === option.id} onClick={() => setFilter(option.id)}
-        className={cn("focus-ring inline-flex h-[var(--control-height)] items-center rounded-control px-3 text-xs transition-colors duration-fast", filter === option.id ? "bg-[rgb(var(--surface-elevated))] font-semibold text-[rgb(var(--foreground))]" : "text-[rgb(var(--text-muted))] hover:bg-[rgb(var(--surface))]")}>{option.label}<span className="ml-1.5 text-metadata tabular-nums text-[rgb(var(--text-muted))]">{model.cases.filter((item) => matches(item, option.id)).length}</span></button>)}
-    </div>
-    {visible.length ? <div className="grid min-w-0 border-b border-[rgb(var(--border))] lg:grid-cols-[minmax(0,1.1fr)_minmax(350px,1fr)]">
-      <div className="min-w-0 lg:max-h-[min(720px,65vh)] lg:overflow-y-auto lg:overscroll-contain">
-        <p className="px-3 py-3 text-metadata text-[rgb(var(--text-muted))]">Ordine de interven╚øie ┬À selecteaz─â un caz</p>
-        <ul aria-label="Coada de execu╚øie comercial─â" className="divide-y divide-[rgb(var(--border))]">{visible.map((item) => <li key={item.id}>
-          <button type="button" aria-pressed={selected?.id === item.id} aria-controls="execution-case-detail" onClick={() => setSelectedId(item.id)}
-            className={cn("focus-ring relative flex w-full min-w-0 gap-3 border-l-2 px-3.5 py-4 text-left transition-colors duration-fast", selected?.id === item.id ? "border-[rgb(var(--primary))] bg-[rgb(var(--primary-soft))]" : "border-transparent hover:bg-[rgb(var(--surface))]")}>
-            <span aria-label={item.severity === "critical" ? "Prioritate critic─â" : item.severity === "attention" ? "Necesit─â aten╚øie" : "De urm─ârit"} className={cn("mt-1.5 h-1.5 w-1.5 shrink-0 rounded-full", item.severity === "critical" ? "bg-[rgb(var(--danger-text))]" : item.severity === "attention" ? "bg-[rgb(var(--warning-text))]" : "bg-[rgb(var(--text-muted))]")} />
-            <span className="min-w-0 flex-1">
-              <span className="flex items-baseline justify-between gap-3"><span title={item.organization} className="truncate text-sm font-semibold">{item.organization}</span><span className="shrink-0 text-xs font-medium tabular-nums">{formatProductCurrency(item.value, item.currency)}</span></span>
-              <span title={item.opportunityTitle} className="mt-0.5 block truncate text-xs text-[rgb(var(--text-secondary))]">{item.opportunityTitle}</span>
-              <span className="mt-2 block text-xs leading-5">{item.primaryReason}</span>
-              <span className="mt-2 flex flex-wrap gap-x-3 gap-y-1 text-metadata text-[rgb(var(--text-muted))]"><span>{item.owner.name}</span>
-                {item.overdue ? <span>{item.overdueDays ? "Restant ┬À " + item.overdueDays + (item.overdueDays === 1 ? " zi" : " zile") : "Termen dep─â╚Öit"}</span> : null}
-                <CaseReadiness owner={Boolean(item.owner.id)} action={Boolean(item.nextAction)} dated={Boolean(item.nextAction?.dueAt)} overdue={item.overdue} evidence={item.evidence.length}/>
-              </span>
-            </span>
-            <ChevronRightIcon aria-hidden="true" className="mt-1 h-3.5 w-3.5 shrink-0 text-[rgb(var(--text-muted))]" />
-          </button>
-        </li>)}</ul>
-      </div>
-      <div key={selected?.id} id="execution-case-detail" className="min-w-0 border-t border-[rgb(var(--border))] bg-[rgb(var(--surface))] lg:max-h-[min(720px,65vh)] lg:overflow-y-auto lg:overscroll-contain lg:border-l lg:border-t-0">
-        <p role="status" className="sr-only">{selected ? "Caz selectat: " + selected.organization + " ÔÇö " + selected.opportunityTitle : ""}</p>
-        {selected ? <CaseDetail key={selected.id} item={selected} impactHref={impactLinks[selected.opportunityId]} /> : null}
+
+  const visible = model.cases.filter((item) =>
+    matches(item, filter),
+  );
+
+  const selected =
+    visible.find((item) => item.id === selectedId) ??
+    visible[0] ??
+    null;
+
+  const filterOptions = filters.map((option) => ({
+    ...option,
+    count: model.cases.filter((item) =>
+      matches(item, option.id),
+    ).length,
+  }));
+
+  return (
+    <section
+      aria-labelledby="execution-center-title"
+      className="control-center-primary pt-5"
+    >
+      {/* ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+          EXECUTIVE HEADER
+      ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */}
+
+      <header className="flex flex-wrap items-end justify-between gap-x-8 gap-y-4">
+        <div className="min-w-0 flex-1">
+          <div className="flex items-center gap-2">
+            <span
+              aria-hidden="true"
+              className="h-1.5 w-1.5 rounded-full bg-[rgb(var(--primary))]"
+            />
+
+            <p className="text-micro font-semibold uppercase tracking-[0.12em] text-[rgb(var(--text-muted))]">
+              Control Center
+            </p>
+          </div>
+
+          <h1
+            id="execution-center-title"
+            className="mt-2 text-page-heading font-semibold tracking-[-0.03em]"
+          >
+            Ce necesit─â aten╚øie acum
+          </h1>
+
+          <p className="mt-1 max-w-3xl text-label leading-5 text-[rgb(var(--text-secondary))]">
+            Situa╚øii comerciale ordonate pentru o interven╚øie
+            sigur─â, cu responsabil, motiv ╚Öi valoare estimat─â.
+          </p>
+        </div>
+
+        <Link
+          href="/opportunities"
+          className="focus-ring shrink-0 rounded text-xs text-[rgb(var(--text-muted))] transition-colors hover:text-[rgb(var(--foreground))] hover:underline"
+        >
+          Toate oportunit─â╚øile ÔåÆ
+        </Link>
+      </header>
+
+      {/* ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+          EXECUTIVE METRICS
+      ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */}
+
+      <div className="control-center-truth-strip mt-4 flex flex-wrap gap-2 border-y border-[rgb(var(--border-subtle))] py-3">
+        <Metric
+          value={model.cases.length}
+          label={
+            model.cases.length === 1
+              ? "situa╚øie de revizuit"
+              : "situa╚øii de revizuit"
+          }
+          tone="primary"
+        />
+
+        <Metric
+          value={model.overdueCount}
+          label="cu termen dep─â╚Öit"
+          tone={model.overdueCount ? "attention" : "neutral"}
+        />
+
+        {Object.entries(model.exposure).map(
+          ([currency, amount]) => (
+            <Metric
+              key={currency}
+              value={formatProductCurrency(
+                amount,
+                currency,
+              )}
+              label="expunere estimat─â"
+            />
+          ),
+        )}
       </div>
-    </div> : <div className="border-b border-[rgb(var(--border))] py-10">
-      <h2 className="text-sm font-semibold">{model.cases.length ? "Nicio situa╚øie ├«n acest filtru." : "Nicio interven╚øie ├«n coada disponibil─â."}</h2>
-      <p className="mt-2 text-xs text-[rgb(var(--text-muted))]">{model.cases.length ? "Alege Toate pentru restul situa╚øiilor comerciale." : "Po╚øi continua lucrul din oportunit─â╚øi sau verifica semnalele de mai jos."}</p>
-    </div>}
-    {model.waitingCount > 0 ? <p className="mt-3 text-xs text-[rgb(var(--text-muted))]">{model.waitingCount} {model.waitingCount === 1 ? "situa╚øie a╚Öteapt─â" : "situa╚øii a╚Öteapt─â"} clientul ├«n fereastra de r─âspuns. F─âr─â revenire prematur─â.</p> : null}
-  </section>;
+
+      {model.sourceState === "fallback" ? (
+        <p
+          role="status"
+          className="mt-3 rounded-[0.7rem] border border-[rgb(var(--border))] bg-[rgb(var(--surface-subtle))] px-3 py-2 text-xs leading-5 text-[rgb(var(--text-muted))]"
+        >
+          Afi╚Ö─âm evaluarea disponibil─â din ├«nregistr─âri.
+          Preg─âtirea interven╚øiilor nu este disponibil─â
+          momentan.
+        </p>
+      ) : null}
+
+      {/* ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+          OPERATIONAL ANALYTICS ÔÇö NOW ABOVE THE QUEUE
+      ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */}
+
+      <section
+        className="control-center-analysis mt-5 border-b border-[rgb(var(--border-subtle))] pb-5"
+        aria-labelledby="control-center-exposure-title"
+      >
+        <div className="mb-4 flex flex-wrap items-end justify-between gap-x-8 gap-y-3">
+          <div className="min-w-0">
+            <div className="flex items-center gap-2">
+              <span
+                aria-hidden="true"
+                className="h-1.5 w-1.5 rounded-full bg-[rgb(var(--primary))]"
+              />
+
+              <p className="text-metadata font-semibold uppercase tracking-[0.12em] text-[rgb(var(--text-muted))]">
+                Analiz─â opera╚øional─â
+              </p>
+            </div>
+
+            <h2
+              id="control-center-exposure-title"
+              className="mt-2 text-base font-semibold tracking-[-0.018em]"
+            >
+              Unde este concentrat─â aten╚øia comercial─â
+            </h2>
+
+            <p className="mt-1 max-w-3xl text-xs leading-5 text-[rgb(var(--text-muted))]">
+              Expunerea estimat─â este derivat─â din cazurile
+              deschise ╚Öi termenele lor comerciale. Valorile
+              r─âm├ón distincte de venitul confirmat ╚Öi nu
+              reprezint─â o prognoz─â.
+            </p>
+          </div>
+
+          <div className="shrink-0 text-right">
+            <p className="text-metadata uppercase tracking-[0.08em] text-[rgb(var(--text-muted))]">
+              Stare analizat─â
+            </p>
+
+            <p className="mt-1 text-xs font-medium text-[rgb(var(--text-secondary))]">
+              {formatProductDate(asOf)}
+            </p>
+          </div>
+        </div>
+
+        <ControlCenterVisuals
+          cases={model.cases}
+          fx={fx}
+          asOf={asOf}
+        />
+      </section>
+
+      {/* ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+          EXECUTION QUEUE HEADER + FILTERS
+      ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */}
+
+      <section
+        className="control-center-interventions mt-5"
+        aria-labelledby="execution-queue-title"
+      >
+        <div className="flex flex-wrap items-end justify-between gap-x-6 gap-y-3 border-b border-[rgb(var(--border-subtle))] pb-3">
+          <div>
+            <p className="text-metadata font-semibold uppercase tracking-[0.11em] text-[rgb(var(--text-muted))]">
+              Ordine de interven╚øie
+            </p>
+
+            <h2
+              id="execution-queue-title"
+              className="mt-1 text-sm font-semibold tracking-[-0.012em]"
+            >
+              Situa╚øiile care cer o decizie
+            </h2>
+
+            <p className="mt-1 text-xs text-[rgb(var(--text-muted))]">
+              Selecteaz─â un caz pentru motiv, dovezi ╚Öi
+              urm─âtorul pas.
+            </p>
+          </div>
+
+          <div className="min-w-0">
+            <SegmentedFilter
+              label="Filtreaz─â situa╚øiile"
+              options={filterOptions}
+              value={filter}
+              onChange={setFilter}
+            />
+          </div>
+        </div>
+
+        {visible.length ? (
+          <div className="control-center-workbench product-work-surface mt-3 grid min-w-0 overflow-hidden lg:grid-cols-[minmax(0,1.08fr)_minmax(350px,0.92fr)]">
+            {/* QUEUE */}
+
+            <div className="control-center-case-list min-w-0 lg:max-h-[min(760px,72vh)] lg:overflow-y-auto lg:overscroll-contain">
+              <div className="flex items-center justify-between border-b border-[rgb(var(--border))] px-3.5 py-3">
+                <p className="text-metadata text-[rgb(var(--text-muted))]">
+                  {visible.length}{" "}
+                  {visible.length === 1
+                    ? "situa╚øie vizibil─â"
+                    : "situa╚øii vizibile"}
+                </p>
+
+                <span className="text-metadata text-[rgb(var(--text-muted))]">
+                  Prioritate opera╚øional─â
+                </span>
+              </div>
+
+              <ul
+                aria-label="Coada de execu╚øie comercial─â"
+                className="divide-y divide-[rgb(var(--border))]"
+              >
+                {visible.map((item) => {
+                  const isSelected =
+                    selected?.id === item.id;
+
+                  return (
+                    <li key={item.id}>
+                      <button
+                        type="button"
+                        aria-pressed={isSelected}
+                        aria-controls="execution-case-detail"
+                        onClick={() =>
+                          setSelectedId(item.id)
+                        }
+                        className={cn(
+                          "focus-ring relative flex w-full min-w-0 gap-3 border-l-2 px-4 py-4 text-left transition-colors duration-fast",
+                          isSelected
+                            ? "control-center-case-selected border-[rgb(var(--interaction))] bg-[rgb(var(--interaction-tint)/0.62)]"
+                            : "border-transparent hover:bg-[rgb(var(--surface-subtle))]",
+                        )}
+                      >
+                        <span
+                          aria-label={
+                            item.severity === "critical"
+                              ? "Prioritate critic─â"
+                              : item.severity ===
+                                  "attention"
+                                ? "Necesit─â aten╚øie"
+                                : "De urm─ârit"
+                          }
+                          className={cn(
+                            "mt-1.5 h-1.5 w-1.5 shrink-0 rounded-full",
+                            item.severity === "critical"
+                              ? "bg-[rgb(var(--danger-text))]"
+                              : item.severity ===
+                                  "attention"
+                                ? "bg-[rgb(var(--warning-text))]"
+                                : "bg-[rgb(var(--text-muted))]",
+                          )}
+                        />
+
+                        <span className="min-w-0 flex-1">
+                          <span className="flex items-baseline justify-between gap-4">
+                            <span
+                              title={item.organization}
+                              className="truncate text-sm font-semibold tracking-[-0.01em]"
+                            >
+                              {item.organization}
+                            </span>
+
+                            <span className="shrink-0 text-xs font-semibold tabular-nums">
+                              {formatProductCurrency(
+                                item.value,
+                                item.currency,
+                              )}
+                            </span>
+                          </span>
+
+                          <span
+                            title={item.opportunityTitle}
+                            className="mt-0.5 block truncate text-xs text-[rgb(var(--text-secondary))]"
+                          >
+                            {item.opportunityTitle}
+                          </span>
+
+                          <span className="mt-2.5 block text-xs font-medium leading-5 text-[rgb(var(--foreground))]">
+                            {item.primaryReason}
+                          </span>
+
+                          <span className="mt-2 flex flex-wrap items-center gap-1.5 text-metadata text-[rgb(var(--text-muted))]">
+                            <span
+                              className={cn(
+                                "control-center-meta-chip",
+                                !item.owner.id && "control-center-meta-chip-muted",
+                              )}
+                            >
+                              {item.owner.name}
+                            </span>
+
+                            {item.overdue ? (
+                              <span className="control-center-meta-chip control-center-meta-chip-warning">
+                                {item.overdueDays
+                                  ? `Restant ┬À ${item.overdueDays}${
+                                      item.overdueDays === 1
+                                        ? " zi"
+                                        : " zile"
+                                    }`
+                                  : "Termen dep─â╚Öit"}
+                              </span>
+                            ) : null}
+
+                            <span className="control-center-evidence-chip">
+                              {item.evidence.length}{" "}
+                              {item.evidence.length === 1 ? "dovad─â" : "dovezi"}
+                            </span>
+
+                            <CaseReadiness
+                              owner={Boolean(item.owner.id)}
+                              action={Boolean(item.nextAction)}
+                              dated={Boolean(
+                                item.nextAction?.dueAt,
+                              )}
+                              overdue={item.overdue}
+                              evidence={item.evidence.length}
+                            />
+                          </span>
+                        </span>
+
+                        <ChevronRightIcon
+                          aria-hidden="true"
+                          className={cn(
+                            "mt-1 h-3.5 w-3.5 shrink-0 transition-transform duration-fast",
+                            isSelected
+                              ? "translate-x-0.5 text-[rgb(var(--interaction))]"
+                              : "text-[rgb(var(--text-muted))]",
+                          )}
+                        />
+                      </button>
+                    </li>
+                  );
+                })}
+              </ul>
+            </div>
+
+            {/* SELECTED CASE */}
+
+            <div
+              key={selected?.id}
+              id="execution-case-detail"
+              className="control-center-case-inspector min-w-0 border-t border-[rgb(var(--border))] bg-[rgb(var(--surface))] lg:max-h-[min(760px,72vh)] lg:overflow-y-auto lg:overscroll-contain lg:border-l lg:border-t-0"
+            >
+              <p
+                role="status"
+                className="sr-only"
+              >
+                {selected
+                  ? `Caz selectat: ${selected.organization} ÔÇö ${selected.opportunityTitle}`
+                  : ""}
+              </p>
+
+              {selected ? (
+                <CaseDetail
+                  key={selected.id}
+                  item={selected}
+                  impactHref={
+                    impactLinks[
+                      selected.opportunityId
+                    ]
+                  }
+                />
+              ) : null}
+            </div>
+          </div>
+        ) : (
+          <div className="product-grouping-surface mt-3 px-5 py-10">
+            <h2 className="text-sm font-semibold">
+              {model.cases.length
+                ? "Nicio situa╚øie ├«n acest filtru."
+                : "Nicio interven╚øie ├«n coada disponibil─â."}
+            </h2>
+
+            <p className="mt-2 text-xs leading-5 text-[rgb(var(--text-muted))]">
+              {model.cases.length
+                ? "Alege Toate pentru restul situa╚øiilor comerciale."
+                : "Po╚øi continua lucrul din oportunit─â╚øi sau verifica semnalele de mai jos."}
+            </p>
+          </div>
+        )}
+      </section>
+
+      {model.waitingCount > 0 ? (
+        <div className="mt-3 flex items-start gap-2 border-l-2 border-[rgb(var(--border-strong))] pl-3">
+          <p className="text-xs leading-5 text-[rgb(var(--text-muted))]">
+            {model.waitingCount}{" "}
+            {model.waitingCount === 1
+              ? "situa╚øie a╚Öteapt─â"
+              : "situa╚øii a╚Öteapt─â"}{" "}
+            clientul ├«n fereastra de r─âspuns. F─âr─â revenire
+            prematur─â.
+          </p>
+        </div>
+      ) : null}
+    </section>
+  );
 }
diff --git a/src/components/dashboard/HomeAskSurface.tsx b/src/components/dashboard/HomeAskSurface.tsx
index e397526..381b15d 100644
--- a/src/components/dashboard/HomeAskSurface.tsx
+++ b/src/components/dashboard/HomeAskSurface.tsx
@@ -2,15 +2,24 @@
 
 import { CopilotConversation } from "@/components/intelligence/CopilotConversation";
 
-const suggestions = ["Ce necesit─â aten╚øie ast─âzi?", "Ce s-a schimbat de ieri?", "Ce oportunit─â╚øi nu au urm─âtor pas?", "Unde avem valoare expus─â?"];
+const suggestions = [
+  "Ce decizii cer aten╚øie acum ╚Öi de ce?",
+  "Ce s-a schimbat comercial de la ultima verificare?",
+  "Unde lipse╚Öte responsabilul sau urm─âtorul pas?",
+  "Ce expunere are termenul cel mai apropiat?",
+];
 
 export function HomeAskSurface({ greeting }: { greeting: string }) {
   return (
-    <section className="mx-auto w-full max-w-4xl pt-4 sm:pt-8" aria-labelledby="home-greeting">
-      <div>
-        <h1 id="home-greeting" className="text-2xl font-semibold tracking-[-0.03em] text-[rgb(var(--foreground))] sm:text-[1.75rem]">{greeting}</h1>
+    <section className="control-center-ask-surface mx-auto w-full max-w-5xl" aria-labelledby="home-greeting">
+      <div className="flex flex-wrap items-start justify-between gap-3">
+        <div>
+          <p className="text-metadata font-semibold uppercase tracking-[0.11em] text-[rgb(var(--primary))]">Analist comercial</p>
+          <h2 id="home-greeting" className="mt-1 text-base font-semibold tracking-[-0.02em] text-[rgb(var(--foreground))]">{greeting}</h2>
+        </div>
+        <p className="max-w-xs text-right text-metadata leading-4 text-[rgb(var(--text-muted))]">Context autorizat ┬À f─âr─â ac╚øiune extern─â automat─â</p>
       </div>
-      <div className="mt-5">
+      <div className="mt-3">
         <CopilotConversation lockedContext={{ pageType: "dashboard" }} contextLabel="Control Center" initialSuggestions={suggestions} />
       </div>
     </section>
diff --git a/src/components/dashboard/ShellNavigation.tsx b/src/components/dashboard/ShellNavigation.tsx
index f7908b5..e0eafae 100644
--- a/src/components/dashboard/ShellNavigation.tsx
+++ b/src/components/dashboard/ShellNavigation.tsx
@@ -35,11 +35,11 @@ export function ShellNavigation({ items, onNavigate, variant = "sidebar", ariaLa
                     "focus-ring group relative flex items-center rounded-control border border-transparent font-medium transition-[background-color,border-color,color] duration-fast ease-standard",
                     variant === "sidebar" ? "min-h-8 gap-2 px-2 text-label" : "min-h-9 gap-3 px-3 text-sm",
                     active
-                      ? "border-[rgb(var(--border)/0.72)] bg-[rgb(var(--surface-selected)/0.82)] text-[rgb(var(--foreground))] before:absolute before:inset-y-1.5 before:left-0 before:w-0.5 before:rounded-full before:bg-[rgb(var(--intelligence))]"
+                      ? "border-[rgb(var(--interaction-border)/0.72)] bg-[rgb(var(--interaction-tint)/0.72)] text-[rgb(var(--foreground))] before:absolute before:inset-y-1.5 before:left-0 before:w-0.5 before:rounded-full before:bg-[rgb(var(--interaction))] dark:border-[rgb(var(--border)/0.72)] dark:bg-[rgb(var(--surface-selected)/0.82)] dark:before:bg-[rgb(var(--intelligence))]"
                       : "text-[rgb(var(--text-muted))] hover:bg-[rgb(var(--surface-subtle))] hover:text-[rgb(var(--foreground))]"
                   )}
                 >
-                  <NavigationIcon name={item.icon} className={cn(variant === "sidebar" ? "h-[17px] w-[17px]" : "h-[18px] w-[18px]", "shrink-0", active ? "text-[rgb(var(--text-secondary))]" : "text-[rgb(var(--text-muted))] group-hover:text-[rgb(var(--foreground))]")} />
+                  <NavigationIcon name={item.icon} className={cn(variant === "sidebar" ? "h-[17px] w-[17px]" : "h-[18px] w-[18px]", "shrink-0", active ? "text-[rgb(var(--interaction))] dark:text-[rgb(var(--text-secondary))]" : "text-[rgb(var(--text-muted))] group-hover:text-[rgb(var(--foreground))]")} />
                   <span className="truncate">{item.name}</span>
                 </Link>
               );
diff --git a/src/components/dashboard/Sidebar.tsx b/src/components/dashboard/Sidebar.tsx
index dd49564..c20e313 100644
--- a/src/components/dashboard/Sidebar.tsx
+++ b/src/components/dashboard/Sidebar.tsx
@@ -19,11 +19,23 @@ export function Sidebar({
   userName?: string;
 }) {
   return (
-    <aside className="fixed inset-y-0 left-0 z-40 hidden w-[224px] overflow-visible border-r border-[rgb(var(--border-strong)/0.82)] bg-[rgb(var(--sidebar))] lg:flex lg:flex-col" aria-label="Navigare ReveNew">
-      <div className="border-b border-[rgb(var(--border))] bg-[rgb(var(--sidebar))] px-2 py-1.5">
-        <WorkspaceMenu businessName={businessName} userEmail={userEmail} userName={userName} isDemo={isDemo} variant="sidebar" />
-      </div>
-      <div className="border-b border-[rgb(var(--border)/0.72)] px-2 py-1.5"><GlobalSearch /></div>
+    <aside
+  className="authenticated-sidebar fixed inset-y-0 left-0 z-40 hidden w-[224px] overflow-visible border-r border-[rgb(var(--border-strong)/0.82)] bg-[rgb(var(--sidebar))] lg:flex lg:flex-col"
+  aria-label="Navigare ReveNew"
+>
+  <div className="border-b border-[rgb(var(--border))] bg-[rgb(var(--sidebar))] px-2 py-1.5">
+    <WorkspaceMenu
+      businessName={businessName}
+      userEmail={userEmail}
+      userName={userName}
+      isDemo={isDemo}
+      variant="sidebar"
+    />
+  </div>
+
+  <div className="border-b border-[rgb(var(--border)/0.72)] px-2 py-1.5">
+    <GlobalSearch />
+  </div>
 
       <div data-sidebar-primary className="app-scrollbar min-h-0 flex-1 overflow-y-auto px-2 pb-1.5 pt-1.5">
         <ShellNavigation items={primaryItems} />
